# DECISION_LOG: namioshi v3

この文書は、namioshi v3で後続作業が勝手に別仕様へ変わらないようにするための決定ログである。現在実装ではなく、今後のv3改修で守る契約を記録する。

1. 正式表示名とgame_slugはnamioshiを維持する。
2. 1プレイは30秒とする。短時間でルールを理解できないという実機の指摘を受け、10秒から変更した。
3. プレイヤーが発生できる根の波は最大6回とする。時間を増やすだけでなく、反射経路を試す判断回数も増やす。
4. ランキング対象の公式モードと、ランキング対象外の練習モードを分ける。
5. 公式モードは全員が同じ配置条件で競う。
6. 練習モードだけランダム配置を許可する。
7. ゲーム内部座標は端末のピクセル数へ直接依存させない。
8. v3の基準論理座標は360×640とする。
9. 反射は最大2回とする。
10. 同じタップ由来の波と同じビーコンの組み合わせは、最も高い候補得点だけを採用する。
11. 公式スコアでは全体コンボ倍率を使わない。
12. 純粋WebGLを正式な高品質描画候補として維持する。
13. WebGLが使えない場合はCanvas 2Dへ切り替える。
14. Three.jsは必須ではない。
15. Three.jsへ移行する場合は、別Phaseで比較試作と実機測定を行う。
16. 全作品共通の3MB上限を設けない。
17. 容量ではなく、入力反応、フレーム時間、継続動作、実機確認で合否を決める。
18. Codeberg Pagesを標準公開先とする。
19. ランキングは既存の共通submit_score RPCを使用する。
20. Publishable keyをAuthorization: Bearerへ入れない。
21. secret keyとservice_role keyをブラウザへ入れない。
22. 実機未確認を確認済みと書かない。
23. v3の得点式や公式配置を公開した後に変更する場合は、旧ランキングと混ぜない。
24. 現在の旧スコアが存在するかは未確認事項として扱う。
25. 実験場トップと詳細ランキングの登録方式は、現在の実装を確認してから決める。
26. 効果音は初期設定で「なし」とし、利用者がHOMEで「あり」へ変更した場合だけ鳴らす。
27. ビーコン命中の精度は、中心への近さを0から1で表した値により、PERFECT（0.85以上）、GREAT（0.60以上）、GOOD（0.30以上）、HIT（0.30未満）の4段階へ分ける。
28. 命中音は判定が良いほど高く、音数を増やす。タップ、壁反射、ガラス反射、2回反射命中、結果表示も聞き分けられる音にする。
29. 初回プレイでは時間制限なしの案内を表示し、反射板へ波を当てる体験を先に行えるようにする。案内結果はランキングへ送らない。
30. 画面内の棒は「反射板」と表示し、外側の光、両端の印、反射方向の目印、接触時の光で役割を伝える。
31. 基礎点は直接波20点、壁反射100点、反射板180点、2回反射300点とする。
32. 命中候補は `rootTapId × beaconId` ごとに最高得点だけを採用し、後から高い候補が出た場合は差額だけを加える。全体コンボ倍率は使わない。
33. 結果画面には、直接、壁反射、反射板、2回反射の最終得点内訳を表示する。
34. 物理更新は1/60秒の固定刻みとし、1描画あたり最大3回とする。画面の更新間隔へ物理結果を依存させず、可視中の未処理累積は捨てずに後続描画へ持ち越す。
35. プレイの締切は画面更新回数ではなく、プレイ開始時の単調増加時計から作る。画面非表示中は物理更新と音を止めるが、締切は進める。
36. 敵対的検証でG4「物理と得点」を不合格へ戻す。G4の再合格、時刻処理、描画復旧が終わるまで、ランキングと競技版公開を停止する。
37. 反射得点は、最後の反射面から逆順に有限線分をたどり、すべての交点と順序が有効な場合だけ採用する。同じ経路で同一面を再使用しない。
38. 波の故障検知上限は、壁4面、反射板4面、深度2、同一面再使用なし、根波6個から導く390波とする。端末性能を理由に得点可能な経路を黙って削らない。
39. HIGH、MID、LOWの品質差で得点判定する波を隠さない。品質を下げる時は、解像度、粒子、背景装飾から調整する。
40. ビーコンの基準運動と命中による一時揺れを分け、命中で基準速度を増加させない。一時揺れは上限、ばね、減衰により基準軌道へ戻す。
41. 波の移動、反射、得点は3秒の寿命内に限定する。1回の更新が寿命をまたぐ場合は残り寿命までしか処理しない。
42. 決定34の「余分な累積を捨てる」は、時刻付き入力と締切跨ぎで端末差を生むため撤回する。1描画3stepを保ちながら未処理stepを保持し、締切時刻を固定して残りを精算する。
43. 決定37の有限経路と同一面再使用禁止は、旧18論理ピクセル再判定距離を置き換える。同じ波から同じ面への子波は1回だけ、同じ祖先経路で同一面は再使用しない。
44. 反射は固定step終端だけで判断せず、接触半径と接触時刻を使う。子波は接触半径から同じstepの終端まで伝播し、同一step内の2面目と寿命最終slice内の正当な反射・得点を処理する。寿命に達した親子波はstep終了後に残さない。
45. 新規子波の生成時点で次面の仮想最短半径をすでに越えていても、その面を潜在経路から除外しない。早い仮想接触の光・音は描画復旧工程で有効弧と一致させ、得点は有限経路の逆順検査だけで確定する。
46. 入力時刻の区間はプレイ開始を含み30秒締切を含まない半開区間とする。入力は単調時計の時刻と内部採番順で保持し、入力時刻以上の最初の固定境界へ到達した後、step終端で適用する。
47. 締切をまたぐ描画では`min(now, deadline)`だけを固定step供給へ使い、締切後は同じdeadlineでaccumulatorを最大3stepずつ排出する。accumulatorが1step未満になった時だけ一度RESULTへ進む。
48. `visibilitychange`または`pagehide`へ移る時点までの可視時間と入力は保持し、非表示中だけ物理を停止する。締切前の通常復帰では固定境界と保留入力を非表示時間分だけ後ろへ移す。締切後に復帰する場合も非表示前の可視accumulatorを捨てず、境界と入力時刻を非表示開始から締切までの分だけ移してから同じ締切時刻で排出する。移動後の時刻が締切以上になる入力は破棄する。
49. 命中帯中はwave×beaconごとに最小error候補を保留し、帯を抜けるか寿命最終sliceで一度だけ台帳・揺れ・onHitを確定する。完全一致だけは即時確定し、候補更新による通知多重発火を許可しない。
50. 公式配置candidate-c-open-harborの固定fixture（137/1073/2249/3377/4193/5281ms、3ビーコン反復）は、20/30/60/120Hzおよび可視中の50/100/150/1000msの長い描画間隔で同一結果となる。実到達例はスコア5481、内訳は直接0・壁239・反射板429・2回反射4813で、台帳合計・内訳合計とも5481、固定step数は1800、かつ6480以下である。
51. 30秒締切は命中候補の第3の確定境界とする。締切までに観測した最小error候補だけを物理を進めず一度確定し、未到達候補は作らない。公式の遅延入力fixture `(0,110,27025ms)` は確定前358点、締切確定後589点となり、20/30/60/120Hzで一致する。
52. 画面休止で次の固定境界が締切後へ移っても、移動後の時刻が締切未満である受理済み入力は消さない。締切までの固定step精算後に、その入力を締切終端で保存順に一度だけ適用してから保留命中を確定する。終端適用では物理を進めず、新しい得点候補を作らない。
53. `visibilitychange`と`pageshow`が同じ復帰を連続通知しても、固定境界、入力時刻、wall-clock cursorを二重に移さない。復帰通知では物理stepを排出せず、次の描画ループで1描画最大3stepの契約を守る。
54. 反射深度がある波は、得点と同じ`traceReflectionPath`の有限経路契約で描画点を検査し、有効な弧だけを表示する。直接波は全円のままとし、96分割は描画サンプリングであって物理上限ではない。
55. 親の有限経路で到達不能な仮想接触でも、後続の有効な二面経路を保持するため子波は残す。反射光と`onReflect`通知は`waveCanReach`が真の接触だけへ出し、無効な接触の画面・音通知を抑制する。
56. WebGLコンテキスト消失時は可視時間を精算して入力と描画を一時停止し、復帰時に描画資源を再初期化する。復帰または描画中のWebGL処理に失敗した場合は、同じcanvasで2D contextを取得せず新canvasへ差し替えてCanvas 2Dへ切り替える。World、得点、入力キューは保持する。

## 2026-08-11 回復D第一段階: ランキング安全化を未有効化で着手

- Pull Request #45（回復C）の統合後、次工程をランキング安全化とする。
- 正式Project URLは読み取り専用確認で https://mlpnjgezrnhdxsxolyzj.supabase.co と確認した。コード内のPublishable keyは同Projectの有効なキーと一致した。キーの実値は記録しない。
- クライアントのランキング送信状態は enabled=false のまま維持する。
- SQL、RPC、RLS、public.games、本番データは変更しない。
- クライアント側へ公式モード限定、play ID、スコア範囲、重複防止、10秒中断、apikeyのみ、古い応答抑制を追加する。これらはサーバー検査の代わりにならない。


## 2026-08-11 回復D第一段階の検証結果

- Draft Pull Request #46を作成した。
- GitHub Actions Run #73（ID 31469136374）はNode.js 18・20・22の全ジョブが成功した。
- Actions内のnpm testは96/96、verifyも成功した。
- 既存テストが要求する「RPC契約前はmain.jsから送信を呼ばない」条件を維持した。
- ランキング送信、SQL、RPC、RLS、public.games、本番データは変更していない。

57. 回復D D2では、既存共有ランキングを変更せず、namioshiの記録をprivateスキーマへ分離する未適用SQLをレビュー対象とする。
58. 7引数版の同名submit_score RPCを提案するが、既存4引数版は変更せず、PostgRESTのオーバーロード解決は非本番で確認するまで未確定とする。
59. D2の初期設定はenabled=false、season=prelaunch-v1、verification_status=self_reportedとし、self-reported記録を競技性の証明と扱わない。
60. play_idは同一内容の再送を冪等化し、異なる内容の再送を拒否する。競合直列化と名前単位の頻度制限を置くが、本人性・不正防止の証明とは扱わない。
61. SQL適用、本番限定疎通、ランキング表示接続は、非本番検証とユーザーの明示承認を経た別工程とする。


62. 回復D D2の7引数RPCは、SupabaseのRPCで関数名のオーバーロードが使えないため、既存の4引数`public.submit_score`を変更せず、専用名`public.submit_namioshi_score_v1`へ修正する。クライアントの送信先とSQL提案・検査を同じ専用名へそろえ、SQL適用とランキング有効化は引き続き未承認・未実施とする。

## 2026-08-12 Phase 6Aの優先

Supabaseの非本番検証は後回しにし、依存しないPhase 6Aの端末内保存と結果画面を先に進める。公式モードの`bestScore`と公式・練習の`playCount`を分け、保存できない環境では保存済みと表示しない。ランキング送信、ランキング表示、Supabase本番・非本番の変更は行わない。


## 2026-08-12 Phase 6Bの範囲

PR #49の統合後は、Supabase連携を後回しにしたままPhase 6Bの画面・支援機能を進める。safe-area、横画面、読み上げの意味づけ、フォーカス表示、`prefers-reduced-motion`を対象とし、ゲームルール、得点、物理、描画方式、端末保存、ランキングは変更しない。

safe-areaと横画面はCSSで余白と配置を調整し、画面回転や`visualViewport`の変更では360×640のWorldを作り直さず、viewportと描画サイズだけを再計算する。実機での表示確認は自動検査と分け、iPhone・iPadでのsafe-area、横画面、キーボード、VoiceOver、外部キーボードは未確認として残す。


## 2026-08-12 Phase 7Aの描画土台

63. WebGLの品質設定は背景波と粒子へ適用し、得点対象の前景波はHIGH・MID・LOWのいずれでも隠さない。attribute・uniform位置、単位円のsin/cos、動的頂点バッファは初期化時に準備する。プレイ中の品質は変更せず、HOME・RULES・COUNTDOWN・RESULT・ERRORは最大30fpsで描画する。これは描画負荷を抑える判断であり、ゲームルール、得点、物理、ランキング、Supabaseの契約を変更するものではない。

58. Phase 7Bでは、波の見た目をWebGLの`lineWidth`だけで表さず、反射の可視範囲を保った帯状ジオメトリで描画する。Canvas 2Dは同じ有限反射弧を二重線で描き、描画方式の違いで判定情報を欠落させない。
59. Phase 7Bの命中表示は、実際に加点された正の差分だけを表示し、候補が確定していない状態や加点0の状態を成功表示として扱わない。

## 2026-08-12 Phase 7C 音と振動

64. Phase 7Bの統合後は、既存の効果音を作り直さず、音なしでも画面表示だけで遊べる契約を維持したまま、任意の振動通知を追加する。
65. 振動は初期設定で「なし」とし、利用者がHOMEで有効にした場合だけ端末へ保存する。利用者が無効にした場合は、進行中の振動も停止する。
66. `navigator.vibrate`の有無を実行時に確認する。非対応環境では振動ボタンを「非対応」と表示し、入力、反射、命中、結果、復帰の処理を止めない。iPhone Safariで対応しているとは仮定しない。
67. タップ、壁反射、ガラス反射、命中判定、2回反射命中、結果表示は別の振動パターンを使う。ただし、振動がなくても得点、画面表示、音声処理は変えない。
68. `visibilitychange`、`pagehide`、`pageshow`では音声と振動を表示状態へ合わせる。停止・復帰でWorld、入力時刻、得点、物理時間を変更しない。

## 2026-08-12 Phase 8A 自動試験

69. Phase 7CはPull Request #53の統合で完了扱いとし、次はPhase 8Aの不足している自動契約を補う。Phase 7D相当のWebGL復旧は回復C（Pull Request #45）でコード契約まで実装済みのため、同じ描画実装を重ねない。
70. シェアは、本文と公式URL、ネイティブ共有の成功・キャンセル・予期しない失敗、クリップボードのみ、共有機能なし、コピー失敗をNode.js標準テストで固定する。共有経路の失敗は結果画面の手動コピー導線へ渡す前提を維持する。
71. HOME、RULES、COUNTDOWN、PLAYING、RESULT、ERRORの状態画面は一つのstate値から表示を切り替え、カウントダウンとプレイ開始の二重起動を拒否する契約を自動試験で確認する。
72. Chromium、WebKit、Playwrightの実行環境がないためブラウザ操作試験は追加しない。Node試験の成功を実ブラウザ・iPhone・iPad確認済みとは扱わず、G4、G7、Supabase、ランキング再開、公開判定は未完了のまま残す。


## 2026-08-12 Phase 8A統合後・Phase 8B開始

- Pull Request #54をmainへ統合し、Phase 8Aの共有経路・画面導線の自動試験を完了扱いとする。GitHub Actions Run #91はNode.js 18・20・22の全ジョブが成功し、`npm test`は127/127である。
- Node.js標準テストの成功は、実ブラウザ、iPhone・iPad、WebGLコンテキスト消失、Canvas 2D切替、音、振動、safe-area、長時間稼働の確認へ置き換えない。
- Phase 8Bでは同じ公開候補コミット単位で実機・実ブラウザの結果を記録する。利用できない端末は未確認とし、推測で成功結果を作らない。
- `RANKING_SERVICE_STATE.enabled=false`、Supabase未適用、ランキング再開停止、Codeberg Pages公開未確認を維持する。


## 2026-08-12 Phase 8B検収対象の更新

- Pull Request #55の統合コミット `bc038feba7774cc58ecb3ce7845012e98b80eb18` をPhase 8Bの検収対象として固定する。PR #55は文書6ファイルだけを変更しており、ゲームコード、テストコード、ランキング、Supabase設定は変更していない。
- PlaywrightのNode.jsパッケージが利用できても、ChromiumとWebKitの実行バイナリがない場合は、実ブラウザ検収を完了扱いにしない。今回のChromium導入は破損アーカイブで失敗したため、画面、コンソール、性能の証拠を作成しない。
- iPhone・iPadの実機結果がないため、G4・G7・G8、ランキング再開、Phase 8C、Codeberg Pages公開を保留する。


## 2026-08-12 Phase 8B基準コミットの更新

- Pull Request #56のマージコミット `2474ec9e9a4863e483465075cfaa294d696feb58` を、現在のmainとPhase 8B検収対象として扱う。
- PR #56は文書6ファイルだけを変更しており、ゲーム本体、自動試験コード、ランキング、Supabase設定は変更していない。
- 実ブラウザ・iPhone・iPadの確認結果は追加されていないため、G4・G7・G8、Phase 8C、ランキング再開、Supabase適用、公開判定は保留する。

## 2026-08-12 公開版Cloud Chromeでの締切遷移失敗

- 公開版 https://chameleonjp-lab.github.io/namioshi/ をCloud Chromeで確認し、公式モードの締切後も「残り 0.0」が35秒以上継続してRESULTへ遷移しない失敗を記録する。
- ゲーム本体URLのコンソールエラーは確認されなかった。これは実ブラウザで確認した失敗であり、iPhone・iPad、WebGL強制消失・復帰、Canvas 2D切替、音・振動、長時間動作の成功を意味しない。
- 原因候補をrendererSuspended中の締切決済停止に限定し、期限到達時の固定更新を継続する最小修正と自動回帰契約をDraft候補へ追加する。ゲームルール、得点、ランキング、Supabase設定は変更しない。

## 2026-08-13 締切精算を画面更新から切り離す

73. PR #58統合後もGitHub PagesのCloud Chromeで「残り 0.0」停止を再現したため、原因をrendererSuspendedだけに限定した判断を撤回する。
74. 1Hz相当では30秒時点で90/1800固定step、未処理1710step、追加570画面更新が必要になる。締切後の未処理stepは画面更新を待たず、最大60論理フレーム・180stepの小分け処理で排出する。
75. 小分け処理の間では`setTimeout(0)`でブラウザへ制御を返す。画面非表示中は停止し、表示復帰後に再開する。締切前の1画面最大3stepは変更しない。
76. 1Hz、20Hz、30Hz、60Hz、120Hzで公式fixtureの得点・内訳・1800stepを一致させる。ランキングとSupabaseは無効のまま維持し、実ブラウザと実機の再検証が終わるまでPhase 8Bを完了扱いにしない。

## 2026-08-13 PR #59統合後・戦略ルール再調整

77. PR #59をmain `4689107`へ統合し、GitHub ActionsとPages配信の成功を確認した。配信後のCloud Chromeで入力なしの公式30秒プレイが`公式結果`へ遷移し、以前の「残り 0.0」停止は再現しなかった。この1プレイをiPhone・iPadや全Phase 8B項目の合格には置き換えない。
78. 「考えなくても高得点になる」原因へ、波速110と経路重複抑制を同じ競技ルール版で適用する。波速110だけでは全ラウンド同地点攻略を十分抑えず、経路抑制だけでは旧波速165のランダム得点が高止まりするため、単独案を採用しない。
79. 得点は、従来の`rootTapId × beaconId`最高1候補を第一段階として維持し、確定済み根タップから`beaconId × 順序付きsurfaceKey列`最高1候補を第二段階で選ぶ。直接波は`direct`とする。第一段階を経路台帳へ完全置換しない。また、第一段階で落ちた低得点の新経路は加点対象にならないことを仕様と画面へ明記する。
80. 根タップの波群終了または30秒締切までは候補を確定せず、確定済み根タップを後から変更しない。新規経路は全候補点、既存経路の高精度更新は差額、更新なしは0点とし、得点を途中で減らさない。
81. 同時に確定した複数候補は、新規件数、更新件数、発見済み件数、合計加点を1件へ集約する。物理的な命中の光・音・振動は候補確定時、経路の加点結果は根タップ確定時に分け、画面へ示す合計加点をHUD増分と一致させる。
82. 競技ルール版を`namioshi-v3-strategy-002`、client versionを`namioshi-v3.2.0-official003`、将来のranking seasonを`prelaunch-v2`とする。配置ID、指紋、配置版`namioshi-v3-layout-study-001`は維持する。旧ルールの端末ベストと初回案内完了キーは新ルールへ引き継がず、旧値を削除しない。
83. Phase 3Bの配置比較は波速165の歴史資料として同じsnapshotとSVGを維持する。現行波速110の評価で上書きせず、別の`namioshi-strategy-analysis-002` snapshotへ、client/rule/layout、seed範囲、時刻列、座標範囲、分位点定義を固定する。
84. 戦略分析はランダムと設計入力を同じ1/6/11/16/21/26秒へそろえる。seed 1〜1000のcalibrationは中央値1803・q90 2355、seed 10001〜11000のholdoutは中央値1881・q90 2432。境界込み429地点と上位周辺378地点の同地点探索最高標本は3106、上位12地点からの2地点交互baselineは3293、異なる6地点の設計fixtureは4362である。標本最大を数学的最大や人の再現性とは扱わない。
85. 設計fixture、同地点最高標本、holdout固定例は20/30/60/120Hzで一致し、総得点、内訳合計、経路台帳合計、画面通知加点合計、1800固定stepを同じ受入条件へ含める。Supabase、SQL適用、ランキング送信・表示は引き続き無効のままとする。
86. 単純な2地点交互攻略との差が将来ほぼ消えても回帰検査を通さないよう、設計fixture / 2地点baseline最高標本を1.25倍以上へ固定する（現行4362 / 3293 = 1.325倍）。同時確定表示の判定と反射種別は集約全体の属性ではなく最高候補の代表値と明記する。

## 2026-08-14 PR #60統合後・初回案内と練習配置

87. Pull Request #60のマージコミット `61f73be4b116761c2dbb08daba3811399e55fd01` を後続作業の基準とする。GitHub Actions Run #103はNode.js 18・20・22の全ジョブが成功した。ランキング、Supabase、Ready化は停止したまま、次の変更を競技ルールPRとは分ける。
88. 初回案内は反射板へのガラス反射を1回確認するまで本番開始ボタンを無効にする。成功前のHOME復帰は許可するが、成功前に案内完了キーを保存しない。成功後は画面状態とボタン文言を更新してから本番へ進める。
89. 練習モードのランダム配置は、反射板の端点をx=18〜342px、y=90〜622pxへ収め、各ビーコン中心から28px以上離す。最大96回の候補探索と制約を満たす固定fallbackを使い、seed 1〜10000で検査する。公式配置の座標・指紋・配置版は変更しない。
90. 今回の受入は自動試験、build、verify、配置分析、SVG、容量、差分検査とDraft PRのActionsまでとする。iPhone・iPad、実ブラウザ、人の初見再現性、ランキング再開、Supabase適用、Ready化、マージは未確認または未承認のまま残す。

## 2026-08-14 PR #61統合後・同時確定フィードバックの明確化

91. PR #61のマージコミット`50d227e8bcbf61d5365d18b40ca574adaf92ab82`を後続作業の基準とする。ユーザー確認前のマージは行わず、ランキングとSupabaseは無効のまま維持する。
92. 同時確定の通知は引き続き1件とし、新規・更新・発見済み件数、合計加点、代表の判定・反射種別を維持する。追加で、集約対象の候補種別数と実際に加点した種別数を分離して通知する。
93. 画面は加点種別の件数を「経路」として表示し、代表の判定・反射種別が集約全体の判定ではないことを「代表」で明示する。加点0の既知通知では候補種別を表示する。
94. 長い集約通知は狭い画面で折り返す。得点、二段階台帳、物理、保存、ランキング、Supabaseの契約は変更しない。
95. 自動検査は、同時通知が1件であること、直接と壁反射など複数種別がそれぞれ集計されること、表示文が種別件数を参照することを固定する。実ブラウザ・実機での読みやすさは未確認として残す。

## 2026-08-14 PR #62統合後・結果画面の経路案内Draft

96. PR #62のマージコミット`1c724ccab97ab7a88bb7e1e55f5a2019a0670086`を後続作業の基準とする。GitHubへのpushとDraft PR作成は行うが、ユーザー確認前のマージは行わない。
97. 結果画面へ、確定済み経路台帳の最高候補を「反射面の順序・判定語・得点」で表示する。直接経路は直接と表示し、比較規則は既存の得点・深度・誤差・波IDを使う。
98. 結果画面の次の目標は1つだけとし、未命中、反射板未使用、2回反射未達、それ以外の順で決定的に案内する。結果表示は得点計算や台帳を変更しない。
99. 最高経路は未確定候補から選ばず、`routeBestHits`の確定済み値だけを対象にする。リセット後は最高経路を表示しない。
100. 自動検査は最高経路の選択・反射順序コピー・リセット時の空値、RESULTの表示要素と次の目標、既存の総得点整合を固定する。実ブラウザ、iPhone・iPad、人の初見再現性、ランキング再開、Supabase、Ready化は未確認または未承認のまま残す。

## 2026-08-14 PR #63統合後・反射板の意味を伝えるプレイ中の案内Draft

101. PR #63のマージコミット`7d527d5846e84e95fb083b1264f6266e28ca37e2`を後続作業の基準とする。GitHubへのpushとDraft PR作成は行うが、ユーザー確認前のマージは行わない。
102. プレイ中の案内は、波・反射板・ビーコンの3項目へ分ける。反射板は「光る線」、ビーコンは「白く光る点」と呼び、役割を短く説明する。
103. 波の円、反射板の線、ビーコンの点を小さな色付き記号で示す。これは見分け方を補助する表示であり、座標、判定、得点、経路台帳へ影響させない。
104. 初回案内とルール画面では「棒に波」という曖昧な表現を使わない。既存の「反射板」という用語と、画面で見える形をそろえる。
105. 自動検査は3項目のラベル、記号、反射板とビーコンの説明、曖昧表現の除去を固定する。実ブラウザ、iPhone・iPad、人の初見理解、ランキング再開、Supabase、Ready化は未確認または未承認のまま残す。

## 2026-08-15 プレイ中の目的・得点・終了条件の表示

106. プレイ中は「波をビーコンに重ねる」を目的として常時表示し、「反射板経由は高得点」と短く補足する。これは説明表示だけの変更で、得点式・物理・経路台帳は変更しない。
107. RULESでは、ビーコンに波が重なると得点になり、反射板に当てるだけでは得点にならないことを明記する。基準点は直接20点、壁100点、反射板180点、2回反射300点とする既存契約を表示へ反映する。
108. 6回のタップを使い切った後も30秒まではプレイを終了しない既存契約を維持し、波が残る場合と消えた場合の待機文をプレイ中へ表示する。表示追加を理由に早期終了を導入しない。
109. 練習モード入口では、30秒・6タップ・反射経路の練習・ランキング外であることを明記する。練習のランダム配置とランキング送信なしの契約は維持する。
110. 目的表示・待機表示・得点説明は音声や色がなくても読めるHTMLテキストとし、画面が狭い場合は折り返す。実ブラウザ・iPhone・iPadでの読みやすさは未確認として扱う。

## 2026-08-15 命中確認と得点確定の区別

111. 物理的なビーコン接触時の通知は「命中確認：判定・経路種別」とし、候補が接触したことを示す。接触時点では二段階台帳の根タップ精算が終わっていないため、ここで加点済みとは表示しない。
112. 根タップの波群が寿命を終えた時、または30秒締切で精算した時の正の差分は「得点確定：+N」と表示し、新規・更新の件数と代表経路を併記する。既知経路の0点通知は「発見済み（得点なし）」と表示する。
113. 命中確認は接触時、得点確定は精算時という役割をRULES、凡例、初回案内、画面通知でそろえる。表示の追加は得点式、物理、二段階台帳、30秒終了条件、ランキング、Supabaseを変更しない。
114. 命中確認は青系の状態表示、得点確定は既存の加点表示とし、音や色が使えない環境でもHTMLテキストで区別できることを自動検査へ含める。狭い画面では通知を折り返す。実ブラウザ・iPhone・iPadの視認性は未確認として残す。

## 2026-08-15 PR #66統合後・10回タップと水系効果音

115. PR #66統合後のmain `c3ec10a6e74467bfd8e8ba727b02c634541d01d2`を基準に、根の波の上限を6回から10回へ変更する。30秒、波速110、寿命3秒、最大2回反射、得点基礎値、公式配置ID・座標・指紋・配置版は維持する。
116. 10回化に伴い、1根波×3ビーコン×最大候補360点の経路上限を10800点へ更新する。client versionは`namioshi-v3.3.0-official004`、競技ルール版は`namioshi-v3-strategy-003`、ranking seasonは`prelaunch-v3`とし、旧6回・6480点の端末ベストとランキング記録を混ぜない。
117. 戦略分析は10入力を1/4/7/10/13/16/19/22/25/28秒へそろえる。seed 1〜1000のcalibrationは中央値2491・q90 3192・最大4084、seed 10001〜11000のholdoutは中央値2562・q90 3235・最大4351、同地点最高標本3271、2地点最高標本3805、設計fixture7629とし、snapshotを正本とする。設計fixtureはrandom中央値の2倍以上、各baselineの1.25倍以上を自動gateする。
118. 効果音を有効化した利用者へ、タップ＝水面の波紋、壁・反射板＝返る波、ビーコン命中＝水しぶき、正の得点確定＝水滴、RESULT＝静かな水面の低音量合成cueを既存判定音へ重ねる。初期オフ、音なしでも読めるHTML表示、visibility/pagehideで停止する契約は維持する。
119. 10回化と水系cueの実ブラウザ・iPhone・iPadでの聞き分け、人の初見再現性、ランキング再開、Ready化は未確認のままとし、Draft PRをマージしない。

## 2026-08-15 反射波タップ技能ボーナス

120. 反射波タップは通常の10根波タップとは別の追加アクションとする。表示中の有限反射弧に近い点を固定stepで判定し、深度1は10〜20点、深度2は20〜40点を精度に応じて加える。
121. 同じ`rootTapId`では1回、ラウンド全体では10件までに制限する。反射波タップは根波や経路を生成せず、同じ波の連打・有限経路外・反射前の全円には加点しない。
122. 経路10800点と技能上限400点を合わせてhard ceilingを11200点へ更新する。client versionは`namioshi-v3.4.0-official005`、競技ルール版は`namioshi-v3-reflection-tap-004`、ranking seasonは`prelaunch-v4`へ分離し、旧v3記録を混ぜない。
123. 反射波タップは`reflectionTap`内訳と「反射波タップ：+N」通知へ集約し、判定音へ`WATER_RIPPLE`を重ねる。音なしでも判定語・深度・点数が分かることを自動受入へ追加する。

## 2026-08-15 PR #68統合後・反射波タップ視認性補助

124. PR #68のマージコミット`5a02f5aa337d848fca1a86f93bc94aaee9ba5c2f`を次のDraftの基準とする。反射波タップの得点、根波10回、経路台帳、ランキング無効状態は維持する。
125. 未使用で寿命内の有限反射弧だけへ淡い黄色の補助表示を重ねる。技能ボーナス取得済み、根波確定済み、寿命終了後の波には表示しない。
126. 補助表示はWorldの反射波タップ対象判定をWebGLとCanvas 2Dで共通利用し、反射弧の形を残す。表示は判定範囲、物理、速度、寿命、得点台帳を変更しない。
127. 補助表示の実ブラウザでの見やすさ、iPhone・iPad、初見再現性、ランキング再開、Supabase適用、Ready化は未確認または未承認とし、Draftをマージしない。

## 2026-08-15 PR #69統合後・反射波タップのタイミング案内

128. PR #69で反射後の有限弧を盤面上で見つけやすくしたため、次は「どこ」だけでなく「いつ重ねてタップするか」をプレイ中に伝える。案内は凡例内の短いHTMLテキストとし、色や音が使えなくても読めるようにする。
129. 案内は未使用の有限反射弧がある間だけ表示し、「黄色い反射弧に重ねてタップ」と深度別の+10〜40点を示す。`World.isReflectionTapAvailable`を共有して、反射タップ済み、根波確定済み、寿命終了後、初回案内中、PLAYING以外では表示しない。
130. この変更は入力キュー、根波10回、反射タップ判定、得点、二段階台帳、物理、水系SEを変更しない。表示の明滅は`prefers-reduced-motion`の既存ルールに従う。
131. 自動試験では、案内要素・文言・共有判定・対象消失時の非表示・CSSを確認する。実ブラウザ、実機、初見再現性、ランキング再開、Supabase、Ready化は未確認として残す。

## 2026-08-16 PR #71統合後・自動検証と正本同期Draft

132. PR #71のマージコミット`4385d14c633c0d7d39c9b8b80b5fa2c277a6ff0c`を現行mainとして扱う。PR #71で、案内・入力・Canvas 2D・WebGLが同じ96中点サンプルによる可視反射弧判定を共有し、反射波タップ全体の上限10件も同じ条件へ含めた。
133. 現行値は30秒、根波10回、波速110、経路10800点＋技能400点の総hard ceiling11200点である。旧6回・6480点は履歴資料に限定し、端末ベストやランキングへ混ぜない。
134. PR #71統合後mainで151テスト、build、verify、配置・戦略分析、SVG、容量、差分検査、敵対的884ケース、GitHub Actions #126を確認した。案内が表示されるのに描画弧がない不一致は0件だった。
135. 現行10入力の戦略snapshotは、calibration中央値2491、holdout中央値2562、同地点最高標本3271、2地点最高標本3805、設計fixture7629である。これらは決定的な自動検証値であり、人の初見再現性や実ブラウザ表示を証明しない。
136. PR #71後の次工程は正本文書の同期だけとし、ゲームコード、得点、物理、入力、描画、音、ランキング、Supabaseを変更しない。実ブラウザ・実機、長時間動作、ランキング再開、Ready化は未確認または未承認のまま、Draftをマージしない。


## 2026-08-16 PR #73・波の寿命と終了条件

137. タップで既存の波を消す処理はなく、見かけ上の消失は約3秒の寿命切れと同じ描画時刻に起こり得る入力反映で説明できる。表示上の途切れには0.18秒の視覚フェードだけを使う。
138. 根波10回、固定stepの追いつき、入力キュー0、物理波0、チュートリアル外を満たした時だけRESULTへ早期精算する。30秒締切と締切時の精算は維持する。
139. PR #73の変更はmain最新へ組み直し、mergeable=trueを確認した。自動テスト153件と関連検査を通過し、マージはユーザー確認まで行わない。


## 2026-08-16 PR #73統合後・次期検収ゲート

140. PR #73のマージコミット`148ff54ea516f24ffe02501fdb4797ff915adad5`を現行mainとして扱う。波の寿命約3秒、既存波をタップで削除しないこと、根波10回後の早期RESULT遷移を現行仕様へ確定した。
141. 自動テスト153件、build、dist検証、配置・戦略分析、レイアウト確認、容量確認、GitHub Actions G2 Build Verification #131を成功として記録する。
142. 実ブラウザ、iPhone・iPad、初見プレイ、音の聞き分け、長時間動作、ランキング再開、Supabase、Ready化は未確認のまま維持する。


## 2026-08-17 PR #74統合後・計画棚卸しと仕様正本化

143. PR #73の実装とPR #74の文書同期を含むmain `148ff54ea516f24ffe02501fdb4797ff915adad5`を現在の基準とする。
144. 当初の3回・10秒、旧6回・6480点の記述は背景・履歴資料であり、現行競技ルールは30秒、根波10回、波速110、波寿命約3秒、総hard ceiling11200点とする。
145. `SPEC_v3.md`の状態遷移記述を、30秒締切に加えて「根波10回、固定step追従、入力キュー0、物理波0で早期RESULT」という現行実装へ一致させた。既存波の表示だけが0件になっても、未処理入力または固定stepが残る間は終了しない。
146. README、MASTER、CURRENT_TASK、REVIEW_CHECKLISTの現行基準と公開先表記を同期した。コード、得点、物理、入力、描画、音、ランキング、Supabaseは変更していない。
147. 実ブラウザ・iPhone・iPad・初見操作・水系SEの聞き分け・長時間動作・ランキング実通信・公開判定は未確認のまま維持し、確認完了までランキング再開とReady化を行わない。
