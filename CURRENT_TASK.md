# CURRENT_TASK: PR #83マージ後・リアルな水面波タップと開始導線

## 基準

- 対象: `chameleonjp-lab/namioshi`
- 現行main: `0abfb5c0481bc5ea0713254d5f0323c308607347`（Pull Request #83のマージコミット）
- 対応ブランチ: `agent/namioshi-real-ripple-input-20260821`
- 次のPull RequestはDraftのままにする
- GitHubへのpush／Draft PR作成は行うが、マージ操作は行わない
- `RANKING_SERVICE_STATE.enabled=false`を維持する
- ローカル自動検査は170件pass。build、dist検証、配置・戦略・SVG・容量・差分検査も通過済み。G2と実ブラウザ／iPhone 17 Pro SafariはDraft公開後に確認する。

## 今回の対応範囲

- 波の色分けを廃止し、円環・有限反射弧・HAMEN水面を同じ水色系の水面表現へ統一する。反射板とビーコンの視認性は維持する。
- 実際に反射が起きた地点へ生成されるHAMENの局所水面波を、反射波タップの第一判定対象へ接続する。固定stepの遅延があっても、pointerdown時の波ID・反射エフェクトIDを入力キューへ保持する。
- 反射が成立した回数をWorldで数え、HUDの「反射」、RESULTの反射回数、反射水面波タップのフィードバックへ反映する。既存の深度別技能ボーナスと上限は維持する。
- COUNTDOWNでは説明を表示し、最初の任意タップは説明を閉じるだけにする。そのタップをWorldへ渡さず、3・2・1・START後にPLAYINGへ遷移するため、開始時の二重波を発生させない。
- `RippleSurface`の反射インパルスは固有IDで一度だけ消費し、同一フレーム／同一地点の重複注入を防ぐ。

## PR #80/#81で提供されたHAMEN情報

- PR #80はHAMEN高さ場リップルの実行デモと視覚リファレンスを追加した。
- PR #81は2バッファ更新式、固定60Hz、100msのフレーム差分上限、4乗フォールオフ、格子上限、法線・鏡面反射の原典を追加した。
- いずれも`src/`、`dist/`、ゲーム物理、得点、ランキング、Supabaseは変更していない。

## 今回の取り込み範囲

- `src/render/ripple-surface.js`へ表示専用の高さ場を追加した。`next=((L+R+U+D)/2-previous)*damping`、Dirichlet境界、HAMENのdrop／drag式、粘性減衰、固定60Hzを使用する。
- 根タップ、実際の反射接触、反射した水面波タップを局所インパルスへ変換し、WebGLの水面テクスチャとCanvas 2Dの透明ハイライトへ反映する。
- 物理・得点判定の円環、有限反射弧、波速110、10回上限、3秒の判定寿命、反射輪10秒表示、hard ceiling11200点は変更しない。
- HIGH/MID/LOWで高さ場格子を48,000/30,000/16,000セル以下へ制限し、表示用以外の処理へ高さ場を渡さない。
- 既存の配置・戦略・SVG・容量検査に加え、反射した水面波の入力、反射回数、説明タップゲート、重複インパルスを自動検査する。実ブラウザとiPhone 17 Pro Safariは未確認のまま残す。

## 今回の修正対象

- 固定stepの待ち時間で反射輪が移動しても、pointerdown時に見えていた反射波を同じ対象として処理できるよう、反射対象の小さなスナップショットを入力キューへ保持する。
- 画面上で短くフェードしている反射輪も、見えている間は同じ判定対象として扱う。
- Safariの画面回転やブラウザ表示領域の変化で描画用viewportが古くなった場合、入力前に描画と同じviewportへ更新する。
- WebGLの高さ場テクスチャを二重反転しないようにし、Canvas 2Dと同じ上下方向で表示する。
- 自動テスト、build、dist検証、差分検査を通過させる。実ブラウザとiPhone 17 Pro Safariは未確認のまま残す。

## 次の目的

反射した水面波のタップ加点・反射回数、開始時の説明タップ、重複波抑制をDraft PRでレビュー可能にし、実機での水面波タップ、WebGL／Canvasの上下方向、長時間動作は別受入ゲートとして記録する。

## iPhone 17 Pro受入手順

1. `https://chameleonjp-lab.github.io/namioshi/`をSafariの通常タブで開き、Draftの対象コミットが公開へ混入していないことを確認する。
2. HOMEでRULESを開き、目的、ビーコン、反射板、反射波タップ、得点、10回後の待機条件を読めることを確認する。
3. COUNTDOWNで説明が表示され、最初の任意タップで説明だけが閉じ、3・2・1・START後にPLAYINGへ移ることを確認する。PLAYINGでは上部ボタンで説明を開閉できることも確認する。
4. 10回以内に直接波、反射板経由、反射した水面波を試し、高さ場のハイライトが前景波を隠さないこと、タップしても既存の波が消えないこと、反射表示が最大10秒まで見えることを確認する。水面波タップの加点と反射回数表示を記録する。
5. 10回を使い切った後、表示中の波と入力の精算を待ってRESULTへ進むことを確認する。波が消えた瞬間に画面が固まらないことも記録する。
6. HOMEで音を有効にし、タップ、反射、命中、得点確定、RESULTの水系SEが区別できるか確認する。音を無効にした場合も、画面文言だけで進行を理解できることを確認する。
7. iPhone 17 Proの縦画面で、説明、反射輪、待機文、RESULTが盤面を隠さず読めることを確認する。異常があればスクリーンショットと再現手順を残す。

## 受入の扱い

- 上記を実施していない項目は「未確認」とする。
- 不具合が見つかった場合は、対象画面、入力順、経過時間、再現率を記録してからコード修正を行う。
- 高さ場の表示不具合がないかを確認する。得点・物理・入力・ランキング・Supabaseへ高さ場を接続しない。
- ランキング、Supabase、本番記録、Ready化、公開判定は、受入結果とユーザー判断がそろうまで停止する。

## 検証手順

```bash
node --test tests/*.test.mjs
node tools/analyze-strategy.mjs --check
node tools/analyze-layouts.mjs --check
node tools/render-layout-previews.mjs --check
node scripts/build.mjs
node scripts/verify-dist.mjs
node scripts/report-size.mjs
git diff --check
```

自動検証の成功は、iPhone 17 ProのSafariでの合格へ置き換えない。
---

# 過去CURRENT_TASK記録（PR #77以前）

## 基準

- 対象: `chameleonjp-lab/namioshi`
- 基準main: `148ff54ea516f24ffe02501fdb4797ff915adad5`（Pull Request #73実装・#74文書同期後）
- 対応ブランチ: `agent/namioshi-reflection-lifetime-guide-toggle-20260817`
- Pull RequestはDraftのままにする
- ユーザー確認前にマージしない
- `RANKING_SERVICE_STATE.enabled=false`を維持する

## 前工程の結果

Pull Request #60で波速110と二段階の経路台帳、#61で初回案内の成功確認と練習配置制約、#62で同時確定時の反射種別集約通知、#63で結果画面の最高経路と次の目標、#64で波・反射板・ビーコンの凡例、#65で目的・得点条件・待機表示、#66で命中確認と得点確定の区別、#67で10回タップと水系SE、#68で反射波タップ技能ボーナス、#69で反射弧の視認補助、#70で反射波タップのタイミング案内、#71で可視反射弧判定の共有をmainへ統合した。

PR #68で、反射後の有限弧を狙ってタップすると、深度1は10〜20点、深度2は20〜40点を精度に応じて得られるようになった。根波の10回制限は消費せず、1根波1回・全体10件まで、経路得点とは別の内訳で管理する。

この自動結果は、実ブラウザ、iPhone・iPad、人が初見で反射弧を発見できることの証明ではない。ランキング、ランキング再開、Supabase、Ready化は停止中である。

## 今回の目的

PR #77では、反射後の輪を表示・反射波タップの対象として最大10秒保持し、カウントダウン中の説明表示とPLAYING中の任意開閉ボタンを実装する。通常の根波と反射子波の物理・得点寿命は3秒、10回タップ、得点式、経路台帳、総hard ceilingは維持し、反射輪が残る間の精算と結果遷移を確認する。実ブラウザ・実機・初見操作は別検収ゲートとして残す。

## 実装範囲

- CURRENT_TASK、README、MASTER、SPEC、REVIEW_CHECKLIST、Phase 8B記録の基準をPR #74統合後へ同期する。
- 通常の根波と反射子波の物理・得点処理は約3秒、反射後の輪は表示・技能ボーナスのタップ対象として最大10秒。既存波の保持、根波10回後の表示中の波を待つ早期精算を現行仕様として記録する。
- カウントダウン中は説明を表示し、PLAYING中は上部ボタンで説明を任意に開閉する。
- 自動テスト153件、build、dist検証、G2 Build Verification #131の成功を証跡へ記録する。
- 実ブラウザ、iPhone・iPad、初見プレイ、音の聞き分け、長時間動作、ランキング再開、Supabase、Ready化は未確認のまま残す。
- ゲームコードは表示寿命・反射波タップ対象・プレイ説明表示を変更する。得点式、物理の到達上限、入力上限、音、保存、ランキング、Supabaseは変更しない。

## 自動受入条件

| 条件 | 受入値 |
|---|---:|
| ルール基準 | 10回タップ、30秒、波速110、根波3秒・反射波最大10秒、hard ceiling11200点 |
| 可視性整合 | PR #71の共有判定と描画用有限弧サンプルの不一致0件 |
| 再現性 | 自動テスト153件、build、verify、配置・戦略分析、SVG、容量、差分検査を成功 |
| CI | GitHub Actions G2 Build Verification #131を成功 |
| 未確認の扱い | 実ブラウザ・実機・初見再現性を自動試験済みへ置き換えない |

## 変更しない範囲

- 波速110、根波寿命3秒・反射波寿命最大10秒、最大2回反射、30秒、根波10回
- 反射波タップの判定範囲18論理px、深度別10〜40点、1根波1回、全体10件
- 二段階台帳、route得点10800点、技能上限400点、総hard ceiling11200点
- 公式配置ID・座標・指紋・配置版、公式・練習の保存分離
- 水系SE、振動、ランキング送信・表示、Supabase、本番データ
- 実機確認を自動試験済みへ置き換えないこと

## 検証手順

```bash
node --test tests/*.test.mjs
node tools/analyze-strategy.mjs --check
node tools/analyze-layouts.mjs --check
node tools/render-layout-previews.mjs --check
node scripts/build.mjs
node scripts/verify-dist.mjs
node scripts/report-size.mjs
git diff --check
```

## 未確認として残す項目

- 実ブラウザでタイミング案内が反射弧タップを理解しやすくすること
- Cloud Browserの公開版では初回案内を表示できたが、canvas外側のクリックを複数回行ってもタップ0/10から進まず、反射板確認を完了できなかった。原因は未確定として実機検収を継続する
- iPhone・iPadの狭い画面で案内、補助表示、通知が読みやすいこと
- 初見3〜5プレイで技能ボーナスの狙い方を理解できること
- 水系SEの聞き分け、WebGL復旧、長時間動作、ランキング再開、Supabase適用、Ready化

## 2026-08-15 反射波タップ視認性補助

124. 未使用の有限反射弧には黄色の低い明滅を重ね、反射波タップの対象を色だけに頼らず形でも追えるようにする。
125. 補助表示はWorldの反射波タップ対象判定を共通に使い、技能ボーナス取得後、根波確定後、寿命終了後には表示しない。
126. WebGLとCanvas 2Dで同じ有限反射弧を描き、補助表示が得点判定の波、速度、寿命、台帳へ影響しないことを自動試験で確認する。
127. PR #68統合後のmainを基準にDraftを作成する。GitHubへのpushとDraft PR作成は行うが、ユーザー確認前にマージしない。

## 2026-08-15 反射波タップのタイミング案内

128. PR #69統合後のmain `771ee93e0d2ff073b762a2095a048f9da6d1e9e0`を次のDraftの基準とする。
129. 未使用の有限反射弧がある間だけ、黄色い反射弧へ重ねてタップするタイミングと、深度別+10〜40点の案内を凡例内へ表示する。対象がなくなったら非表示にする。
130. 案内は`World.isReflectionTapAvailable`を共有し、初回案内、PLAYING以外、反射タップ済み、根波確定済み、寿命終了後には表示しない。得点、物理、判定範囲、入力キュー、台帳は変更しない。
131. Draft PRを作成するが、ユーザー確認前のマージは行わない。実ブラウザ、実機、初見再現性、ランキング再開、Supabase、Ready化は未確認のまま残す。

## 戻し方

## 2026-08-16 PR #70統合後・反射波タップ案内の可視判定修正

132. PR #70のマージコミット`790ae7fd09333e78a95fefaf90a23912c1168e49`を基準に、案内が出るのに黄色い反射弧が描画されない状態を修正する。
133. `World.isReflectionTapAvailable`は、未使用・未確定・寿命内・全体上限未到達に加えて、Canvas 2DとWebGLが使う96個の中点サンプルのうち1本以上が有限反射経路へ到達することを確認する。
134. 可視サンプル判定をゲーム層へ置き、案内・描画・入力が同じ関数を共有する。得点、物理、判定範囲、台帳、根波10回、水系SE、ランキング無効状態は変更しない。
135. 公式配置の`(140,520)`で案内が表示される一方、弧の描画点が0になる再現を回帰試験へ固定する。全体ボーナス上限10件時に案内を出さない条件も試験する。
136. Draft PRを作成するが、ユーザー確認前のマージは行わない。実ブラウザ、実機、初見再現性、ランキング再開、Supabase、Ready化は未確認のまま残す。

このDraftのコミットだけをrevertし、PR #70統合後のmain `790ae7fd09333e78a95fefaf90a23912c1168e49`へ戻す。外部サービスとランキングは無効のため、外部データの戻し作業は発生しない。

## 2026-08-16 PR #71統合後・検収ゲートと正本同期

137. PR #71のマージコミット`4385d14c633c0d7d39c9b8b80b5fa2c277a6ff0c`を現行mainとして記録する。
138. 10回タップ、30秒、波速110、hard ceiling11200点、反射弧の可視判定共有を現行仕様として扱い、旧6回・6480点は履歴資料に限定する。
139. マージ後mainの全151テスト、配置・戦略・SVG・配布物検査、敵対的884ケースを証跡へ残す。案内と描画の不一致は0件だった。
140. Draftでは文書同期だけを行い、ゲームコード、ランキング、Supabase、公開判定を変更しない。ユーザー確認前のマージは行わない。

このDraftのコミットだけをrevertし、PR #71統合後のmain `4385d14c633c0d7d39c9b8b80b5fa2c277a6ff0c`へ戻す。外部サービスとランキングは無効のため、外部データの戻し作業は発生しない。


## 2026-08-16 波の寿命表示と10回後の自動精算（PR #73）

141. 波は約3秒で自然に消える。根波の追加処理は既存の波を削除せず、寿命切れの輪は0.18秒だけ画面上でフェードする。物理、得点、入力判定の寿命は延長しない。
142. 根波を10回使い切り、固定stepが追いつき、入力キューと物理波が空になった時だけ、30秒を待たずRESULTへ進む。チュートリアル中と未処理入力がある場合は早期終了しない。
143. 自動テスト153件、build、dist検証、配置・戦略分析、レイアウト確認、容量確認を通過した。実ブラウザ・iPhone・iPad・初見操作は未確認のまま残す。


## 2026-08-16 PR #73統合後・次期検収ゲート

144. PR #73のマージコミット`148ff54ea516f24ffe02501fdb4797ff915adad5`を現行mainとして記録する。波は約3秒で自然に消え、タップで既存波を削除せず、根波10回後に入力と物理波が空になれば早期にRESULTへ進む。
145. 自動テスト153件、build、dist検証、配置・戦略分析、レイアウト確認、容量確認、GitHub Actions G2 Build Verification #131を成功として記録する。
146. 次の検収は、実ブラウザで10回後の終了表示とRESULT遷移、iPhone・iPadでの波の視認性、初見操作での反射経路理解、音あり設定での水系SEの聞き分けを確認する。
147. ランキング、Supabase、Ready化、公開判定は停止中のままにし、確認結果なしに再開しない。次のDraftは検収結果または追加の再現可能な不具合が得られた時に作成する。

## 2026-08-17 PR #77 Draft・反射波10秒とプレイ説明トグル

148. 反射時に生成される子波は物理・得点寿命3秒を維持し、表示・反射波タップの対象として最大10秒保持する。親波の残り物理寿命を短くせず、表示中の反射輪が残る間だけ結果遷移を待つ。
149. カウントダウン中はプレイ説明を表示し、PLAYING中は上部の「説明を表示／説明を閉じる」ボタンから任意に開閉する。説明を閉じてもゲーム入力、得点、反射判定、終了条件は変えない。
150. 競技ルール版を`namioshi-v3-reflection-wave-lifetime-005`、client versionを`namioshi-v3.5.0-official006`、ranking seasonを`prelaunch-v5`へ分離する。旧端末ベストと旧ランキング記録は混ぜない。
151. 自動受入は、反射輪の表示・タップ対象10秒、物理・得点寿命3秒、根波10回後の表示中の波待機、説明の状態遷移、20/30/60/120Hz一致、dist同期、戦略スナップショットを対象とする。実ブラウザ、iPhone・iPad、人の初見再現性、ランキング再開、Ready化は未確認のまま残す。
