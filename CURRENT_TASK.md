# CURRENT_TASK: Phase 8B 締切精算の第2回復（未完了）

## 基準

- 対象: `chameleonjp-lab/namioshi`
- 基準main: `f10a77159eafed3dce0b347332723a75ac46a882`（Pull Request #58統合後）
- 対応ブランチ: `agent/namioshi-deadline-drain`
- Pull RequestはDraftのままにする
- `RANKING_SERVICE_STATE.enabled=false`を維持する

## 目的

Pull Request #58は描画停止中の締切経路を補修したが、統合後のGitHub Pagesでも公式モードが「残り 0.0」のままRESULTへ進まない失敗を再現した。原因を再調査し、画面更新が少ない環境でも30秒後の精算を短時間で終えられるようにする。実ブラウザ・実機で未確認の項目は合格へ変えない。

## 確認した原因

- 通常の固定更新は、1回の画面更新につき最大3stepに制限されている。
- 画面更新が1秒に1回の条件では、30秒時点で90/1800stepしか処理できず、残り1710stepのために追加570回の画面更新が必要だった。
- Pull Request #58は`rendererSuspended`経路を追加したが、通常経路の低い画面更新頻度から締切精算を切り離していなかった。
- 2026-08-13のCloud Chromeでは公開コードにPR #58の関数が含まれ、ページは可視状態、ゲーム本体由来のコンソールエラーなしでも失敗した。このため、原因を描画停止だけには限定しない。

## 今回の対応

- 締切後の未処理stepだけを、画面更新と別の小分け処理で精算する。
- 1回の小分け処理は最大60論理フレーム、合計180固定stepに制限し、次の処理前にブラウザへ制御を返す。
- 締切前の1画面最大3step、入力順、30秒、最大6タップ、得点式を維持する。
- 画面が非表示になった場合は小分け処理を停止し、表示復帰後に残りを再開する。
- 1Hz相当の回帰試験と、得点・内訳・1800stepの一致を追加する。

## 完了条件

- 1Hz相当で30秒時点に残った1710stepを10回以内の小分け処理で精算できる。
- 公式fixtureが5481点、内訳が直接0・壁239・反射板429・2回反射4813、固定stepが1800で一致する。
- 締切前は1回最大3stepを維持する。
- `npm test`、build、verify、配置分析、SVG、容量、差分検査が成功する。
- Draft反映後のCloud ChromeでHOMEからRESULTまで進むことを確認する。公開版mainの再確認はマージ・配信後に別記録する。
- iPhone・iPad、WebGL強制消失・復帰、Canvas 2D切替、音、振動、長時間試験を未確認のまま明記する。

## 変更しない範囲

- ゲームルール、反射経路、波速、得点式、30秒制限、最大6タップ
- WebGL・Canvas 2Dの描画内容、音、振動、端末内保存
- Supabase URL、Publishable key、SQL、RPC、RLS、本番データ
- ランキング送信・表示の有効化
- Ready化、マージ、実機未確認項目の合格化

## 自動確認結果

- `npm test`: 131/131成功
- build、verify、配置分析、SVG検査、容量報告、差分検査: 成功
- 1Hz fixture: 30秒時点90stepから最大10回の小分け処理で1800stepへ到達し、5481点と既存内訳を維持
- Cloud Chromeは公開中のmainでは失敗を再現できたが、この作業環境のローカルURLはブラウザ側で開けなかった。第2修正候補のブラウザ合格には置き換えない。

## 5視点レビューで次に残す事項

- 同じ地点の6連打で6456点となるため、経路重複加点と波の到達範囲を比較検証する。
- 公式配置のランダム入力1000試行は中央値4988点で、意図しない2回反射が多い。波速100・110・120、または経路単位の得点台帳を同じ条件で比較する。
- 初回案内を反射成功の確認型へ変え、命中判定を音なしでも表示する。
- 練習配置は反射板端点の盤外生成と、ビーコンへの過度な近接を制限する。
- これらはゲームルール変更を伴うため、今回の締切不具合とは分けて次の設計工程で扱う。

## 次の工程

このDraftで自動試験とCloud Chromeの操作確認を行う。締切遷移が直った後もPhase 8Bは完了にせず、PR #58統合後の公開版失敗と修正候補の結果を分けて記録する。その次に、5視点レビューで見つかった「考えなくても高得点になる」問題の比較計画を作る。

## 戻し方

このPull Requestをrevertし、PR #58統合後のmain `f10a77159eafed3dce0b347332723a75ac46a882` へ戻す。ランキングとSupabaseは無効のため、外部データの戻し作業は発生しない。
