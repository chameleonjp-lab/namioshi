# CURRENT_TASK: プレイ中の目的・得点・終了条件の明示（Draft）

## 基準

- 対象: `chameleonjp-lab/namioshi`
- 基準main: `cc577a15896cf8e7f26b4fe1b102987fdb5ce03d`（Pull Request #64統合後）
- 対応ブランチ: `agent/namioshi-play-status`
- Pull RequestはDraftのままにする
- ユーザー確認前にマージしない
- `RANKING_SERVICE_STATE.enabled=false`を維持する

## 前工程の結果

Pull Request #60で波速110と二段階の経路台帳、#61で初回案内の成功確認と練習配置制約、#62で同時確定時の反射種別集約通知、#63で結果画面の最高経路と次の目標、#64で波・反射板・ビーコンの凡例をmainへ統合した。各PRのNode.js 18・20・22 Actionsは成功済みである。

この自動結果は、実ブラウザ、iPhone・iPad、人が初見で経路を発見できることの証明ではない。ランキング、ランキング再開、Supabase、Ready化は停止中である。

## 今回の目的

前PRで追加した凡例だけでは、初心者が「何をすれば得点か」「6回後に何を待つか」を判断しにくい。プレイ中の目的、得点条件、6回後の待機状態を短い表示で伝え、練習モードの入口とRULESの説明も同じ内容へそろえる。得点式、経路台帳、物理、競技ルールの数値は変更しない。

## 実装範囲

- プレイ中の案内を、波・反射板・ビーコンの3項目へ分ける。
- 各項目へ、ゲーム内の見た目に対応する小さな色付き記号を付ける。
- 反射板は「光る線」と呼び、波が当たると向きが変わる役割を明記する。
- ビーコンは「白く光る点」と呼び、波を重ねる対象であることを明記する。
- 初回案内とルール画面から、意味が曖昧な「棒」という表現を削除する。
- 自動試験で3項目のラベル、記号、狭い画面でも折り返せる表示契約を固定する。
- プレイ中に「波をビーコンへ重ねる」という目的を常時表示する。
- 6回使い切った後は、波の結果を待ち、30秒終了時に結果へ進むことを表示する。
- RULESへ「ビーコン命中が得点」「反射板命中だけでは得点にならない」「基準点」を明記する。
- 練習モード入口へ「30秒・6タップ・ランキング外・反射経路の練習」を明記する。

## 自動受入条件

| 条件 | 受入値 |
|---|---:|
| プレイ中の項目 | 波・反射板・ビーコンの3項目 |
| 反射板の説明 | 光る線、波の向きが変わる |
| ビーコンの説明 | 白く光る点、波を重ねる |
| 曖昧な表現 | プレイ中・初回案内・ルール画面に「棒に波」を残さない |
| 記号 | 波の円、反射板の線、ビーコンの点 |
| 目的表示 | 波をビーコンへ重ねる、反射板経由は高得点 |
| 6回後の表示 | 波の結果を待ち、30秒終了時に結果を表示 |
| 得点説明 | ビーコン命中、反射板だけでは0、基準点4種 |
| 練習入口 | 30秒・6タップ・反射経路の練習・ランキング外 |
| 既存自動試験 | すべて成功 |

## 変更しない範囲

- 波速110、寿命3秒、最大2回反射、30秒、最大6タップ、得点基礎値、6480点hard ceiling
- 公式配置ID、配置指紋、配置版、競技ルール版、練習配置制約、初回案内の成功必須化
- 二段階台帳の上限と経路選抜順序、同時確定通知、結果画面の得点表示
- 公式・練習の保存分離、ランキング送信・表示、Supabase SQL/RPC/RLS、本番データ
- iPhone・iPad、実ブラウザ、音・振動、WebGL復旧、長時間動作の実機合格
- Ready化、ユーザー確認前のマージ

## 検証手順

```bash
npm test
npm run build
npm run analyze:layouts
npm run analyze:strategy
npm run render:layouts
npm run verify
npm run size
git diff --check
```

生成物`dist`はbuild後に差分がなく、Draft ActionsのNode.js 18・20・22で同じ検査を通すことを受入条件とする。

## 未確認として残す項目

- 実ブラウザで目的・得点・6回後の表示が音なしで読めること
- iPhone・iPadの狭い画面で案内がゲーム盤面を隠しすぎないこと
- 初見3〜5プレイで反射板とビーコンの役割を理解できること
- ランキング再開、Supabase適用、Phase 8B/8C/8Dの完了

## 戻し方

このDraftのコミットだけをrevertし、PR #64統合後のmain `cc577a15896cf8e7f26b4fe1b102987fdb5ce03d`へ戻す。外部サービスとランキングは無効のため、外部データの戻し作業は発生しない。
