# CURRENT_TASK: 同時確定フィードバックの明確化（Draft）

## 基準

- 対象: `chameleonjp-lab/namioshi`
- 基準main: `50d227e8bcbf61d5365d18b40ca574adaf92ab82`（Pull Request #61統合後）
- 対応ブランチ: `agent/namioshi-route-feedback`
- Pull RequestはDraftのままにする
- ユーザー確認前にマージしない
- `RANKING_SERVICE_STATE.enabled=false`を維持する

## 前工程の結果

Pull Request #60で波速110と二段階の経路台帳を統合し、Pull Request #61で初回案内のガラス反射成功確認と練習配置の安全制約を統合した。#61のNode.js 18・20・22 Actionsは成功済みで、mainの木はこの作業ブランチの基準へ反映されている。

## 今回の目的

同時に確定した複数の候補について、集約通知1件の中で反射種別を失わないようにする。得点式、二段階台帳、代表判定、根タップの確定順序は変更しない。

## 実装範囲

- `World.settleRoots()`が、集約対象の候補種別数と実際に加点した種別数を通知へ含める。
- 画面の経路通知は、合計加点と新規・更新・発見済み件数に加え、反射種別ごとの件数を表示する。
- 判定語と代表種別は、集約全体の判定と誤解されないよう従来どおり「代表」と明示する。
- 長い集約文が狭い画面で切れないよう、フィードバック領域の折返しを固定する。

## 自動受入条件

| 条件 | 受入値 |
|---|---:|
| 同時確定の通知 | 1件 |
| 通知の集約項目 | 新規・更新・発見済み・合計加点 |
| 種別内訳 | 候補種別数と加点種別数を別フィールドで保持 |
| 複数種別表示 | 直接・壁反射・反射板・2回反射を件数付きで表示 |
| 代表値 | 判定語・代表種別を維持 |
| 総得点整合 | 総得点=内訳合計=経路台帳合計 |
| 既存自動試験 | すべて成功 |

## 変更しない範囲

- 波速110、寿命3秒、最大2回反射、30秒、最大6タップ、得点基礎値、6480点hard ceiling
- 公式配置ID、配置指紋、配置版、練習配置制約、初回案内の成功必須化
- 二段階台帳の上限と経路選抜順序
- 公式・練習の保存分離、ランキング送信・表示、Supabase SQL/RPC/RLS、本番データ
- iPhone・iPad、実ブラウザ、音・振動、WebGL復旧、長時間動作の実機合格
- Ready化、ユーザー確認前のマージ

## 検証手順

```bash
npm test
npm run build
npm run analyze:layouts
npm run analyze:strategy
npm run render:layouts
npm run verify
npm run size
git diff --check
```

生成物`dist`はbuild後に差分がなく、ActionsのNode.js 18・20・22で同じ検査を通すことを受入条件とする。

## 未確認として残す項目

- 実ブラウザで同時複数種別の1.4秒通知を音なしで読めること
- iPhone・iPadの狭い画面で折返しと案内を確認すること
- 初見3〜5プレイで経路原理を発見・再現できること
- ランキング再開、Supabase適用、Phase 8B/8C/8Dの完了

## 戻し方

このDraftのコミットだけをrevertし、PR #61統合後のmain `50d227e8bcbf61d5365d18b40ca574adaf92ab82`へ戻す。外部サービスとランキングは無効のため、外部データの戻し作業は発生しない。
