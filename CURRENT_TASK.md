# CURRENT_TASK: 結果画面の最高経路・次の目標（Draft）

## 基準

- 対象: `chameleonjp-lab/namioshi`
- 基準main: `1c724ccab97ab7a88bb7e1e55f5a2019a0670086`（Pull Request #62統合後）
- 対応ブランチ: `agent/namioshi-result-guidance`
- Pull RequestはDraftのままにする
- ユーザー確認前にマージしない
- `RANKING_SERVICE_STATE.enabled=false`を維持する

## 前工程の結果

Pull Request #60で波速110と二段階の経路台帳、#61で初回案内の成功確認と練習配置制約、#62で同時確定時の反射種別集約通知をmainへ統合した。各PRのNode.js 18・20・22 Actionsは成功済みである。

この自動結果は、実ブラウザ、iPhone・iPad、人が初見で経路を発見できることの証明ではない。ランキング、ランキング再開、Supabase、Ready化は停止中である。

## 今回の目的

結果画面で、今回もっとも得点に寄与した確定経路と、次に試す目標を1つだけ示す。結果画面の説明を追加するだけで、得点式、経路台帳、物理、競技ルールを変更しない。

## 実装範囲

- `World.getBestRoute()`で、確定済みの経路台帳から既存の候補比較規則に従う最高経路を1件返す。未命中なら`null`を返す。
- RESULTに、最高経路の反射面を順序どおり（例: `壁→反射板`）、既存の判定語、得点を表示する。直接経路は`直接`と表示する。
- RESULTに、状態に応じた次の目標を1つだけ表示する。未命中はビーコン命中、反射板未使用は反射板、2回反射未達は2回反射、それ以外は別経路の探索・最高経路更新を案内する。
- 既存の総得点、内訳、発見済み経路数、端末ベスト保存、ランキング状態は変更しない。
- 狭い画面でも最高経路と目標が折り返されるよう結果表示の色と文字組みを追加する。

## 自動受入条件

| 条件 | 受入値 |
|---|---:|
| 最高経路の対象 | 確定済み経路台帳だけ |
| 最高経路の選択 | 既存の得点・深度・誤差・波IDの決定規則 |
| 経路表示 | 反射面の順序、判定語、得点を含む |
| 命中なし | 「最高経路：まだありません」 |
| 次の目標 | 常に1つ、状態から決定的に選ぶ |
| 得点整合 | 表示追加による総得点・内訳・台帳の変更なし |
| 既存自動試験 | すべて成功 |

## 変更しない範囲

- 波速110、寿命3秒、最大2回反射、30秒、最大6タップ、得点基礎値、6480点hard ceiling
- 公式配置ID、配置指紋、配置版、競技ルール版、練習配置制約、初回案内の成功必須化
- 二段階台帳の上限と経路選抜順序、同時確定通知の集約契約
- 公式・練習の保存分離、ランキング送信・表示、Supabase SQL/RPC/RLS、本番データ
- iPhone・iPad、実ブラウザ、音・振動、WebGL復旧、長時間動作の実機合格
- Ready化、ユーザー確認前のマージ

## 検証手順

```bash
npm test
npm run build
npm run analyze:layouts
npm run analyze:strategy
npm run render:layouts
npm run verify
npm run size
git diff --check
```

生成物`dist`はbuild後に差分がなく、Draft ActionsのNode.js 18・20・22で同じ検査を通すことを受入条件とする。

## 未確認として残す項目

- 実ブラウザで最高経路の反射順序と判定語を音なしで読めること
- iPhone・iPadの狭い画面で結果表示が折り返され、次の目標が1つに見えること
- 初見3〜5プレイで経路原理を発見・再現できること
- ランキング再開、Supabase適用、Phase 8B/8C/8Dの完了

## 戻し方

このDraftのコミットだけをrevertし、PR #62統合後のmain `1c724ccab97ab7a88bb7e1e55f5a2019a0670086`へ戻す。外部サービスとランキングは無効のため、外部データの戻し作業は発生しない。
