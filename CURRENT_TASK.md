# CURRENT_TASK: Phase 8B 初回案内・練習配置の改善（未完了）

## 基準

- 対象: `chameleonjp-lab/namioshi`
- 基準main: `61f73be4b116761c2dbb08daba3811399e55fd01`（Pull Request #60統合後）
- 対応ブランチ: `agent/namioshi-guide-practice`
- Pull RequestはDraftのままにする
- `RANKING_SERVICE_STATE.enabled=false`を維持する

## 前工程の結果

Pull Request #60で、波速110と二段階の経路台帳をmainへ統合した。GitHub Actions Run #103はNode.js 18・20・22の全ジョブに成功している。戦略分析では、設計fixtureが4362点、holdout中央値が1881点、同地点最高標本が3106点、2地点交互baseline最高標本が3293点となった。

この自動結果は、実ブラウザ、iPhone・iPad、人が初見で経路を発見できることの証明ではない。ランキング、Supabase、Ready化は停止中である。

## 今回の目的

PR #60で保留した次の2点を、競技ルール変更とは分けて実装する。

1. 初回案内を「読んだら進める」状態から、反射板への命中を1回確認してから本番へ進む状態へ変える。
2. 練習用ランダム配置で、反射板の端が盤外へ出たり、ビーコンへ近づきすぎたりしないよう制約する。

公式配置、公式ルール、得点式、30秒、6タップ、波速110、ランキング設定は変更しない。

## 実装範囲

- 初回案内中は「反射板に波を当てる」まで本番開始ボタンを無効にする。
- ガラス反射を検出したら、画面上に成功を表示し、本番開始ボタンを有効にする。
- 案内からホームへ戻る操作は、成功前でも使える。成功前に本番へ進む操作は拒否する。
- 練習配置の反射板は、端点をx方向18px以上、y方向90〜622pxに収める。
- 各反射板と各ビーコンの中心距離を28px以上にする。
- 乱数seedを同じにすれば同じ配置になり、異なるseedでは別配置になる性質を維持する。
- 96回の候補探索で配置できない場合は、制約を満たす固定候補へ切り替える。

## 自動受入条件

| 条件 | 受入値 |
|---|---:|
| 練習配置の検査seed | 1〜10000 |
| 反射板のx端点 | 18〜342px |
| 反射板のy端点 | 90〜622px |
| ビーコン中心から反射板まで | 28px以上 |
| 同一seedの再現性 | 完全一致 |
| 初回案内の成功前本番遷移 | 起きない |
| ガラス反射後の本番ボタン | 有効になる |
| 既存自動試験 | 138件以上すべて成功 |

## 変更しない範囲

- 公式配置ID、配置指紋、配置版、競技ルール版
- 波速110、寿命3秒、最大2回反射、得点基礎値、6480点のhard ceiling
- 公式の端末ベストと練習結果の保存分離
- ランキング送信・表示の有効化
- SupabaseのSQL、RPC、RLS、本番データ
- iPhone・iPadの実機合格、実ブラウザの全面合格、Ready化、マージ

## 完了条件

- `npm test`、build、verify、配置分析、SVG、容量、差分検査が成功する。
- 10000個の練習seedで反射板端点とビーコン距離の制約を満たす。
- 初回案内の成功前後のボタン状態が自動検査で固定される。
- 旧165速度の配置履歴とPR #60の戦略分析snapshotを変更しない。
- Draft Pull RequestのGitHub ActionsがNode.js 18・20・22で成功する。

## 次の工程

このDraft後に、必要なら同時複数命中の集約表示、狭い画面での折返し、人が初見3〜5回で異なる経路を見つけられるかを確認する。実機確認を必須の待ち時間にはせず、未確認項目として記録したまま自動検査を先に進める。

## 戻し方

このPull Requestだけをrevertし、PR #60統合後のmain `61f73be4b116761c2dbb08daba3811399e55fd01`へ戻す。外部サービスとランキングは無効のため、外部データの戻し作業は発生しない。
