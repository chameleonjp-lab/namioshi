# CURRENT_TASK: Phase 8B 戦略ルール再調整（未完了）

## 基準

- 対象: `chameleonjp-lab/namioshi`
- 基準main: `4689107`（Pull Request #59統合後）
- 対応ブランチ: `agent/namioshi-strategy-rules`
- Pull RequestはDraftのままにする
- `RANKING_SERVICE_STATE.enabled=false`を維持する

## 前工程の結果

- PR #59の締切精算修正をmainへ統合し、GitHub ActionsとPages配信の成功を確認した。
- 配信後のCloud Chromeで公式モードを開始し、入力なしの30秒プレイが`公式結果`へ遷移することを確認した。
- 以前の「残り 0.0」で停止する失敗は、この公開mainでは再現しなかった。
- これはiPhone・iPad、描画復旧、音・振動、長時間動作の合格を意味しない。Phase 8Bは未完了のまま維持する。

## 今回の目的

旧ルールは波速165、寿命3秒のため、1タップの到達半径が最大495pxとなり、場所をあまり考えなくても盤面全域へ反射波が届いた。同じ地点の6連打でもhard ceiling近くへ届くため、波速だけ、または経路台帳だけの単独変更ではなく、次の2点を同じ競技ルール版で組み合わせる。

1. 波速を165から110論理px/秒へ下げ、意図しない盤面全域到達を減らす。
2. 現行の`rootTapId × beaconId`最高1件を残したうえで、`beaconId × 順序付き反射経路`も最高1件へ抑える。

経路キーは直接波を`direct`として明示し、反射波は通過した`surfaceKey`の順序を含める。既存の根タップ台帳を経路台帳へ置き換える方式は得点を膨張させるため採用しない。

## 実装範囲

- `WAVE_SPEED=110`を共通設定としてWorldと戦略分析で使う。
- 根タップの波群が終了した時、または30秒締切で根タップ候補を確定してから経路台帳を再構築する。
- 第一段階を通過した新しい有効ルートは全点、同じルートの高精度更新は差額、更新なしは0点とする。
- HOME、初回案内、RULES、PLAYINGへ「同じ経路は最高点だけ」を表示する。
- 画面上へ「新ルート」「精度更新」「発見済み」と命中判定を表示し、RESULTへ発見ルート数を追加する。
- 配置版と競技ルール版を分離する。配置IDと指紋は維持し、競技ルール版、client version、将来のranking seasonを更新する。
- 旧ルールの端末ベストを新ルールへ引き継がず、旧保存値は削除しない。
- 固定fixture、配置分析snapshot、比較SVG、仕様、決定ログ、ランキング未適用契約を同期する。

## 自動受入条件と実測

| 条件 | 上限・下限 | 実測 |
|---|---:|---:|
| calibration seed 1〜1000の中央値 / q90 | 2,100 / 2,600以下 | 1,803 / 2,355 |
| holdout seed 10001〜11000の中央値 / q90 | 2,100 / 2,600以下 | 1,881 / 2,432 |
| holdoutの2回反射得点比率中央値 | 25〜45% | 39.84% |
| 境界込み粗429点＋上位周辺378点の全ラウンド同地点最大 | 3,300以下 | 3,106 |
| 設計入力 / 同地点最大 | 1.25倍以上 | 1.404倍 |
| 上位12地点からの2地点交互baseline最高標本に対する設計入力 | 1.25倍以上 | 1.325倍（3,293→4,362） |
| 設計入力 | 4,200以上 | 4,362 |
| 設計入力 / holdout中央値 | 2倍以上 | 2.319倍 |
| 20/30/60/120Hz | 同点 | すべて4,362点 |
| 得点の途中減少 | 0回 | 0回 |

ランダムと設計入力は同じ1/6/11/16/21/26秒を使う。設計入力は人の実機プレイ結果ではなく、30px格子探索から作った固定fixtureである。同地点最大と2地点baselineも連続座標全体の数学的最大ではない。初見の人が経路を発見・再現できるかは未確認として残す。

## 変更しない範囲

- 30秒、最大6タップ、波寿命3秒、最大2回反射、1/60秒固定step
- 配置ID`candidate-c-open-harbor`、配置指紋`fnv1a-fc71e804`
- 得点基礎値、6480点の構造上hard ceiling
- Supabase本番、SQL適用、RPC、RLS、本番データ
- ランキング送信・表示の有効化
- Ready化、マージ、実機未確認項目の合格化

## 完了条件

- `npm test`、`npm run analyze:strategy`、配置分析、SVG、build、verify、容量、差分検査が成功する。
- 総得点、内訳合計、経路台帳合計が一致する。
- ルート順序の違いを別経路とし、直接波を明示キーにする試験が成功する。
- Draft Pull RequestのGitHub Actionsが成功する。
- 修正候補の実ブラウザ表示は、利用可能な公開URLで別途確認する。未確認なら未確認と記録する。

## 次の工程

このDraftの自動検証と独立レビューを終えた後、初見3〜5プレイで「場所と順番を考える」差が出るかを実機で確認する。初回案内の成功必須化と練習ランダム配置の制約は、このルール変更へ混ぜず次の独立候補とする。

## 戻し方

このPull Requestだけをrevertし、PR #59統合後のmain `4689107` へ戻す。新ルール用の端末ベストキーは旧キーと分離され、ランキングとSupabaseは無効のため、外部データの戻し作業は発生しない。
