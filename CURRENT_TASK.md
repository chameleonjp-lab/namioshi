# CURRENT_TASK: 命中確認と得点確定の区別（Draft）

## 基準

- 対象: `chameleonjp-lab/namioshi`
- 基準main: `550b09b8d1e04124001d8cf3812c218854a11441`（Pull Request #65統合後）
- 対応ブランチ: `agent/namioshi-impact-settlement`
- Pull RequestはDraftのままにする
- ユーザー確認前にマージしない
- `RANKING_SERVICE_STATE.enabled=false`を維持する

## 前工程の結果

Pull Request #60で波速110と二段階の経路台帳、#61で初回案内の成功確認と練習配置制約、#62で同時確定時の反射種別集約通知、#63で結果画面の最高経路と次の目標、#64で波・反射板・ビーコンの凡例、#65で目的・得点条件・6回後の待機表示をmainへ統合した。各PRのNode.js 18・20・22 Actionsは成功済みである。

この自動結果は、実ブラウザ、iPhone・iPad、人が初見で経路を発見できることの証明ではない。ランキング、ランキング再開、Supabase、Ready化は停止中である。

## 今回の目的

PR #65で目的と得点条件は説明されたが、命中の瞬間と得点が台帳へ確定する瞬間の違いが画面文言からは読み取りにくい。接触時は「命中確認」、根タップの波群精算時は「得点確定」と明示し、同じ経路の再発見は「得点なし」と表示する。得点式、経路台帳、物理、競技ルールの数値は変更しない。

## 実装範囲

- 命中時の表示を「命中確認：判定・経路種別」とし、物理接触の確認であることを明示する。
- 根タップ精算時の正の加点を「得点確定：+N」とし、新規・更新の件数と代表経路を併記する。
- 既知経路の再発見は「発見済み（得点なし）」とし、別経路を探す案内を残す。
- 凡例、初回案内、RULESで、ビーコン命中後に精算時へ得点が確定する流れを説明する。
- 命中確認と得点確定を色とHTMLテキストの両方で区別し、音なしでも読めるようにする。
- 自動試験で両表示の文言、既知経路の0点表示、狭い画面で折り返せるCSS契約を固定する。

## 自動受入条件

| 条件 | 受入値 |
|---|---:|
| 命中表示 | 「命中確認：」で接触時の判定と種別を表示 |
| 得点表示 | 正の加点は「得点確定：+N」で表示 |
| 既知経路 | 「発見済み（得点なし）」と表示 |
| 役割説明 | ビーコンは白い点、波が重なると命中し精算時に得点確定 |
| ルール説明 | 命中確認と精算時の得点確定を分ける |
| 画面区別 | 命中確認は青系、得点確定は既存の加点表示で区別 |
| 既存自動試験 | すべて成功 |

## 変更しない範囲

- 波速110、寿命3秒、最大2回反射、30秒、最大6タップ、得点基礎値、6480点hard ceiling
- 公式配置ID、配置指紋、配置版、競技ルール版、練習配置制約、初回案内の成功必須化
- 二段階台帳の上限と経路選抜順序、同時確定通知、結果画面の得点表示
- 公式・練習の保存分離、ランキング送信・表示、Supabase SQL/RPC/RLS、本番データ
- iPhone・iPad、実ブラウザ、音・振動、WebGL復旧、長時間動作の実機合格
- Ready化、ユーザー確認前のマージ

## 検証手順

```bash
npm test
npm run build
npm run analyze:layouts
npm run analyze:strategy
npm run render:layouts
npm run verify
npm run size
git diff --check
```

生成物`dist`はbuild後に差分がなく、Draft ActionsのNode.js 18・20・22で同じ検査を通すことを受入条件とする。

## 未確認として残す項目

- 実ブラウザで命中確認と得点確定が音なしで区別できること
- iPhone・iPadの狭い画面で長い精算通知がゲーム盤面を隠しすぎないこと
- 初見3〜5プレイで命中から得点確定までの流れを理解できること
- ランキング再開、Supabase適用、Phase 8B/8C/8Dの完了

## 戻し方

このDraftのコミットだけをrevertし、PR #65統合後のmain `550b09b8d1e04124001d8cf3812c218854a11441`へ戻す。外部サービスとランキングは無効のため、外部データの戻し作業は発生しない。
