# CURRENT_TASK: namioshi Phase 4A 反射処理の土台

## 今回の目的

公開版を実際にプレイした結果、次の問題が確認された。

- 10秒では、ルールを理解する前に結果画面へ進む。
- タップ回数が少なく、反射を考えて試す余地が弱い。
- 画面内の棒が何をする物か分からない。
- 適当にタップした場合と反射を狙った場合の得点差が弱い。

これを受け、ゲームの中心体験を説明し、反射を狙う判断が得点へ反映される状態へ更新する。

## iPhone確認

ユーザーが現行公開版をiPhoneで確認し、「確認問題なし」と報告した。これにより、G3で停止していたPhase 4Aの開始条件を満たしたものとして扱う。ただし、固定更新、画面休止復帰、WebGLコンテキスト復帰、ランキング通信は別の未完了工程として残す。

## 基準

- 対象: `chameleonjp-lab/namioshi`
- 基準ブランチ: `main`
- 基準コミット: `19d207d365826dd6a02c74e2f34701cfbf461d54`（Pull Request #38マージ後）
- Pull Request #37（30秒・6タップと反射板戦略）はmainへマージ済み
- Pull Request #38（PR #37後の計画同期）はmainへマージ済み
- 変更はmainへ直接入れず、下書きPull Requestで提出する。
- ランキング送信、Supabase、公式配置の座標は変更しない。
- iPhone実機確認を自動試験で完了扱いにしない。

## 今回のPhase 4A実装

- 壁と反射板を、実際の有限線分として判定する。
- 反射板の射影が線分外にある場合は、延長線上で反射させず、実際の端点まで到達した時だけ端点反射を行う。
- 波へ `waveId`、`rootTapId`、`parentWaveId`、`previousRadius`、`lifetime`、`energy`、`reflectionDepth`、`reflectedBy`、`surfaceHistory` を持たせる。
- 反射子波は、親波の現在の半径、年齢、寿命を引き継ぎ、半径1・年齢0から再開しない。
- 壁反射はエネルギーを0.72倍、反射板反射は0.84倍にする。得点判定の位置は変えない。
- 同じ反射面は18論理ピクセルの移動距離を使うまで再判定しない。
- 波IDをWorld単位で管理し、同じ条件でresetした時に同じ順序から始める。
- 波の上限到達時は古い波を削除せず、新しい波を拒否して走査中の波を飛ばさない。

## 今回の実装

### 遊ぶ時間と入力

- 1プレイを10秒から30秒へ変更した。
- 根の波を3回から6回へ変更した。
- 7回目以降のゲーム入力は無視する。
- 6回の入力を使い切っても、残り時間が0になるまでRESULTへ進まない。

### 初回案内

- 初回だけ、時間制限なしの案内プレイを表示する。
- 案内中は反射板へ波を当てて試せる。
- 「案内を終えて本番へ」で30秒プレイへ進み、「ホームへ戻る」で中断できる。
- 案内を終えた状態だけを同じ端末へ保存する。保存できない場合もゲームは停止しない。

### 反射板の説明

- 棒を画面上の文言で「反射板」と呼ぶ。
- 反射板へ発光、両端の印、反射方向の目印を付ける。
- 接触時に接触点から光と方向を表示する。
- WebGLとCanvas 2Dの両方で同じ意味が伝わる表示にする。

### 得点

| 種類 | 基礎点 |
|---|---:|
| 直接 | 20 |
| 壁反射 | 100 |
| 反射板 | 180 |
| 2回反射 | 300 |

命中精度は既存の4段階判定を使い、候補得点は基礎点へ精度補正を加える。全体コンボ倍率は使わない。

同じ `rootTapId` と `beaconId` の組み合わせでは、最高候補だけを採用する。低い候補をすでに加点している場合に高い候補が出たときは、差額だけを加え、結果内訳も古い種類から新しい種類へ置き換える。

### 結果画面

- 直接、壁反射、反射板、2回反射の4項目を表示する。
- 4項目の合計と最終スコアが一致する状態を試験する。

## 確認済み

- `npm test`: 42件成功
- 30秒の終了条件、6タップ制限、初回案内の存在を確認
- 直接・壁・反射板・2回反射の基礎点を確認
- 同じタップとビーコンで最高候補だけが最終内訳へ残ることを確認
- 低い候補から高い候補への差額更新を確認
- 7回目以降の入力を無視し、結果内訳の合計が最終スコアと一致することを確認
- `npm run build`: 成功
- `npm run verify`: 成功
- `npm run analyze:layouts`: 成功
- `npm run render:layouts`: 成功
- `npm run size`: 成功。容量は報告値であり、固定上限で失敗させていない
- `src`から`dist/assets`を生成し、公開用ファイルを同期した
- Pull Request #37のマージコミットをmainの基準として確認した
- Pull Request #38のマージコミットをmainの基準として確認した
- iPhone確認について、ユーザーから問題なしの報告を受けた
- 有限線分、端点距離、親子波、寿命継承、エネルギー減衰、World単位の波ID、24波上限を自動試験した

## 今回変更していない範囲

- Supabase URL、Publishable key、RPC、ランキング送信
- 公式配置の座標、配置ID、配置指紋
- シェア処理
- 効果音の既存契約と音声停止・復帰処理
- WebGLの本命・Canvas 2Dフォールバックという構成
- 固定更新、単調増加時計、画面休止復帰
- Supabase、ランキング送信、本番データ

## 未確認

- iPhone Safariの長時間・反復・発熱試験。
- 初回案内の保存が再読み込み後も残るか。
- WebGLとCanvas 2Dの表示差が大きくないか。
- 60Hz相当と120Hz相当で反射系列と得点が一致するか。
- 固定更新、画面休止復帰、WebGLコンテキスト復帰。
- Codeberg Pages、iPhone、iPad、実Supabase通信。

## 後続停止条件

Pull Request #37で、30秒・6タップ、初回案内、反射板の意味表示、最高候補方式の得点台帳、結果内訳をmainへ反映した。ユーザーのiPhone確認によりG3の停止条件は解除した。この変更ではPhase 4Aの有限線分、親子波、寿命継承、面履歴、反射エネルギーを実装するが、Phase 4Cの固定更新と画面休止復帰、ランキング送信、本番公開は開始しない。

## 次の作業

1. このPhase 4A変更を独立レビューし、Draft PRの検査結果を確認する。
2. マージ後にPhase 4Cの固定更新、単調増加時計、画面休止復帰へ進む。
3. Phase 4とPhase 5の契約が完了するまで、ランキング送信と本番公開を開始しない。

## 戻し方

このPull Requestだけを取り消す場合は、今回の変更提案をrevertする。Supabaseへ新しいスコアを送っていないため、ランキングデータの戻し作業は不要である。
