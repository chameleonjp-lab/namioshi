# namioshi v3 完成までの統合計画

- 文書状態: 実装・検収・公開の統合計画
- 作成日: 2026-07-12
- 基準リポジトリ: `chameleonjp-lab/namioshi`
- 基準ブランチ: `main`
- 初版基準コミット: `8bdd6f3b79c7b1c72637c3f81b1b67cb8d978d5f`
- 進捗反映基準: `b329fb1ebcb1c19af3a12e436de9ec6855a03f52`（Pull Request #86マージ後のmain）
- 目標リリース: `namioshi v3.0.0`
- 公開先候補: GitHub Pages の `https://chameleonjp-lab.github.io/namioshi/`（公開判定未確認）
- 正式表示名: `namioshi`
- `game_slug`: `namioshi`

この文書は、v3を完成させるまでの作業順、変更範囲、検証方法、停止条件、承認条件、公開条件を一つにまとめた実行計画である。

現行ゲーム仕様の唯一の正本は`docs/SPEC_v3.md`である。`DECISION_LOG.md`は変更理由と履歴、この文書は工程と状態、`CURRENT_TASK.md`は現在のPull Request、`docs/REQUIREMENTS_v3.md`と`docs/IMPLEMENTATION_PLAN_v3.md`は初期背景・過去計画として扱う。背景資料や過去計画がSPECと衝突する場合は、現行判断へ使わない。

仕様を変える必要が生じた場合は、実装を先に変えず、`docs/SPEC_v3.md`を更新し、変更理由を`DECISION_LOG.md`とPull Requestへ記録する。

## 2026-08-26 PR #86マージ後・実機受入ゲート

- 現行mainは`b329fb1ebcb1c19af3a12e436de9ec6855a03f52`（PR #86マージ後）である。
- PR #86で、描画が遅れて初めて観測した寿命内の反射エフェクトを固有IDで一度だけHAMENへ投入する同期修正をmainへ統合した。
- 次のDraftは、現在の実装、171件の自動検査、未確認の実機範囲を文書へ同期する。再現可能な不具合がない限り、ゲームコード、得点、物理、入力、描画、音、保存、ランキング、Supabaseは変更しない。
- iPhone 17 Pro Safariで、長いフレーム・表示領域変更後の水面波、リアル水面波タップ、反射回数、10回後のRESULT、表示中波の精算、音・振動、説明トグル、縦画面の視認性を確認する。
- 自動検査を実機合格へ置き換えず、ランキング、Supabase、Ready化、公開判定は実機受入とユーザー判断まで停止する。

## 2026-08-26 PR #85マージ後・次期表示同期

- 現行mainは`4cea23ea7257d0618ba1a8be184d8f9997e90a8a`（PR #85マージ済み）である。
- PR #85で、リアル水面波だけを反射タップ対象とし、旧反射弧フォールバックと初回ガラス反射必須条件を撤廃した。
- 次のDraftでは、反射エフェクトを描画側が発生から遅れて初めて観測しても、寿命内なら一度だけHAMENへ投入する。物理・得点・入力上限は変更しない。
- 自動検査が成功しても、iPhone 17 Pro Safariの実表示、長いフレーム後の水面波、反射タップ、10回後のRESULT、音・振動、長時間動作は未確認のまま扱う。
- ランキング、Supabase、Ready化、公開判定は、実機検収とユーザー判断がそろうまで停止する。

## 2026-08-20 PR #82 Draft候補・HAMEN表示専用水面

PR #80/#81のGrok/HAMEN資料を実装へ反映し、2バッファ高さ場を表示専用で追加した。固定60Hz、100ms差分クリップ、4乗フォールオフ、粘性減衰、Dirichlet境界、法線・鏡面ハイライトを共有レイヤーへ集約し、WebGLとCanvas 2Dへ同じ水面情報を渡す。

取り込みの停止境界は明確である。高さ場は`World`の円環半径、有限反射経路、ビーコン判定、得点候補、経路台帳、入力キュー、結果遷移に接続しない。高さ場は根タップ、実反射接触、反射波タップの視覚インパルスだけを受ける。品質別格子上限はHIGH 48,000、MID 30,000、LOW 16,000セルとし、前景の得点波を隠さない。

自動テスト162件、build、dist整合性、配置・戦略・レイアウト・容量検査、G2 Build Verification #174のNode.js 18/20/22は通過。iPhone 17 Pro Safariの受入は未確認であり、PRはDraftのまま、マージとランキング／Supabaseの再開は行わない。

---

## 2026-08-20 表示品質工程の追補（PR #82 Draft）

### 完了

- [済] PR #80/#81のHAMEN原典（2バッファ、固定60Hz、4乗フォールオフ、境界、法線・鏡面反射）を読み、namioshiの円環判定と分離する境界を決めた。
- [済] `RippleSurface`を追加し、根タップ・実反射接触・反射波タップの視覚インパルスだけを高さ場へ投入した。
- [済] WebGLの低解像度テクスチャとCanvas 2Dの透明ハイライトを同一高さ場から生成した。
- [済] 高さ場が得点・反射経路・入力キュー・終了条件へ影響しない回帰試験、src/dist整合性、buildを通過した。

### 未確認・停止条件

- [未確認] iPhone 17 Pro Safariで水面ハイライトが前景の円環・反射弧・ビーコンを隠さないこと。
- [未確認] WebGLとCanvas 2Dの実機フレーム時間、発熱、30分連続動作、背景復帰。
- [済] G2 Build Verification #174のNode.js 18/20/22（単体テスト、戦略、レイアウト、verify、size、dist再現性）。
- [停止] 高さ場を判定半径、反射経路、得点、ランキング、Supabaseへ接続しない。PRはDraftのまま、マージと公開判定は行わない。

## 1. 完成の定義

v3は、コードが動くだけでは完成としない。次の条件をすべて満たした時だけ完成とする。

### 1.1 ゲームとして完成している

- 1プレイは30秒で終了する。
- プレイヤーが発生できる根の波は最大10回である。
- 11回目以降のゲーム入力は無視される。
- 壁反射とガラス反射を使う意味がある。
- 直接波だけを無造作に置いて高得点を取れる設計ではない。
- 反射回数は最大2回である。
- 同じタップ由来の波と同じビーコンの組み合わせで、得点が重複しない。
- 公式モードでは全員が同じ配置、同じ初期条件、同じ得点式で競う。
- 練習モードのランダム結果はランキングへ送られない。
- 同じ公式配置と同じ論理入力では、端末の画面サイズが違っても同じ結果になる。

### 1.2 操作と画面が完成している

- `HOME / RULES / COUNTDOWN / PLAYING / RESULT / ERROR` が破綻なく遷移する。
- 名前未入力では公式モードを開始しない。
- 入力後にiPhoneのキーボードがゲーム画面へ残らない。
- 320×568 CSSピクセルで、必要なボタンが見切れない。
- safe-area、縦画面、横画面、画面回転へ対応する。
- 結果画面で、最終得点、端末ベスト、プレイ回数、得点内訳、送信状態を確認できる。
- 通信が失敗しても、再挑戦、シェア、終了、実験場への移動を使える。
- 色だけに依存せず、波の種類と得点価値を区別できる。

### 1.3 描画と音が完成している

- WebGL版で、水面、直接波、壁反射、ガラス反射、2回反射、ビーコン、ガラス片、命中演出を見分けられる。
- WebGLの線幅だけへ波の太さを依存させない。
- Canvas 2D版でも、同じゲームルールと必要な視認性を維持する。
- WebGL初期化失敗時にCanvas 2Dへ切り替わる。
- WebGLコンテキスト消失後に復帰するか、安全にCanvas 2Dへ切り替わる。
- タップ、反射、命中、結果の音が区別できる。
- 音を利用できない環境でもゲームが止まらない。
- 音のON/OFFを保存できる。

### 1.4 ランキングが完成している

- 既存の共通 `submit_score` RPCを変更せず、`namioshi`専用RPCで分離する安全案を採用する。専用RPCの適用・ランキング有効化は別承認とする。
- `game_slug` は `namioshi` である。
- 公式モードの結果だけを送る。
- 1プレイにつき送信は1回だけである。
- Publishable keyは `apikey` ヘッダーへだけ設定する。
- Publishable keyを `Authorization: Bearer` へ入れない。
- secret keyとservice_role keyを公開物へ入れない。
- 送信は10秒程度で打ち切る。
- 古いプレイの通信結果が、新しい結果画面を書き換えない。
- v2以前の得点とv3得点を、無条件に同じ順位表へ混ぜない。
- Supabase URL、Publishable key、RPC、`public.games` 登録が同じプロジェクトであることを実通信で確認する。

### 1.5 性能と安定性が完成している

- 60Hz端末では滑らかな60fpsを目標にする。
- 継続して30fpsを下回る状態を公開しない。
- タップから最初の波表示まで50ms以内を目標にする。
- 意図しない100ms以上の停止を公開停止条件とする。
- 高更新端末でも、ゲーム時間、波速度、得点判定が変わらない。
- 連続再挑戦10回で、イベント、タイマー、配列、描画資源が増え続けない。
- 30分連続稼働で、操作不能、表示崩れ、音の多重化、通信の多重化が起きない。
- バックグラウンド移動と復帰を10回繰り返しても復帰できる。
- オフライン時に結果画面が操作不能にならない。
- 同じ公開候補版で、主要検収を3回連続して通す。

### 1.6 公開作業が完成している

- 公開元となる正しいコミットとタグが特定されている。
- `src` が正本で、`dist` は同じコミットから再生成できる。
- Codeberg Pagesで実際に起動する。
- 実験場トップと詳細ランキングから到達できる。
- シェア文の末尾URLからゲームへ到達できる。
- 公開前のバージョンへ戻す手順が文書化されている。
- 使用素材の出所と利用条件を確認している。
- 実機未確認項目が、必須項目として残っていない。

---

## 2. 現在地

初版基準コミットはv2相当だったが、進捗反映基準ではPull Request #71までmainへ反映済みである。下記の各Phase節には、その時点の6回・旧ルールを含む履歴が残るため、現行値は末尾の最新追補を優先する。

### 2.1 完了済み

- v3の要件、仕様、決定、実装順、確認表を文書化した。
- 3MB上限をv3の受け入れ条件から外した。
- 複数ファイルとES Modulesを正式に許可した。
- 見せかけのLOADINGを削除した。
- `RULES` 画面を追加した。
- HTMLの重複IDを解消した。
- カウントダウンの二重起動防止を追加した。
- 名前入力後に `blur()` を実行するようにした。
- 保存していない結果を「保存済み」と表示しないようにした。
- 共有状態とランキング状態を分離した。
- 共有キャンセルと共有失敗を分離した。
- 手動コピー欄へ正式なシェア文だけを表示するようにした。
- Phase 2A・2BでJavaScript正本、依存0件、再現可能なbuild、容量報告専用化を完了した。
- Phase 3Aで360×640固定論理座標とviewport変換を実装した。
- Phase 3B・3B.1で公式配置3候補の分析、比較画像、選定資料を作成した。
- ユーザー判断により候補C・開港型を公式配置へ採用した。
- Phase 3Cで公式固定配置と練習ランダム配置を分離し、Phase 5までランキング送信を停止した。
- Pull Request #29でRESULTからのモード再選択、不透明な開始前画面、不明モード拒否、送信関数入口の停止を補修した。
- Pull Request #29のNode.js 18・20・22の全検査が成功した。
- 320×568相当のローカルChromiumで、rootと`dist`の公式・練習・公式の往復、名前保持、ランキング通信0件を確認した。
- Pull Request #31・#32で、初期無音の任意効果音と判定4段階を含む10種類の効果音を追加した。
- Pull Request #33で、音声再開失敗、画面非表示時の停止、復帰後の古い予約音を補修した。
- Pull Request #34で、同じ1回の壁・ガラス接触から反射通知を二重に出す経路を防いだ。
- Pull Request #35で、当時の旧仕様に対する早期終了を防いだ。その後、Pull Request #37で30秒・6タップへ変更した。
- Pull Request #33〜#35のNode.js 18・20・22の全検査が成功した。
- Pull Request #37で、30秒・6タップ、初回の時間制限なし案内、反射板の意味表示、最高候補だけを採用する得点台帳、結果画面の得点内訳を追加し、mainへマージした。
- Pull Request #38で、Pull Request #37後の計画・確認表・READMEをmainへ同期した。
- ユーザーがiPhoneで現行公開版を確認し、問題なしと報告したため、G3のiPhone確認停止条件を解除した。
- Pull Request #39で、有限線分、端点反射、親子波、寿命継承、面履歴、反射エネルギー、World単位の波ID、24波上限を実装し、mainへマージした。
- Pull Request #40で、固定更新、単調増加時計、画面休止時の停止と復帰を実装し、mainへマージした。
- Pull Request #42で、有限反射経路、寿命、6入力、全判定波表示、ビーコンの一時揺れを回復し、mainへマージした。
- Pull Request #43で、時刻付き入力、未処理step保持、30秒締切精算、最接近得点を実装し、mainへマージした。
- Pull Request #44で、締切確定と画面休止境界の補修を実装し、mainへマージした。
- Pull Request #45で、有限反射弧、WebGL復旧、安全なCanvas 2D切替を実装し、mainへマージした。
- Pull Request #46で、ランキング送信のクライアント安全契約を追加した。ランキングは無効のまま維持した。
- Pull Request #47・#48で、namioshi専用の未適用ランキングサーバー契約とRPC名の分離案を整えた。
- Pull Request #49で、端末内保存と結果画面の終了導線を追加し、mainへマージした。
- Pull Request #50で、safe-area、横画面、読み上げ、フォーカス、reduced-motionのコード契約を追加し、mainへマージした。
- Pull Request #51で、WebGLの描画土台を最適化し、mainへマージした。
- Pull Request #52で、WebGLの波帯、水面、ガラス、ビーコン、命中表示を高品質化し、mainへマージした。
- Pull Request #53で、既存の効果音契約を維持したまま、対応環境だけで使える任意の振動通知を追加し、mainへマージした。
- Pull Request #54で、共有経路と画面導線の自動回帰契約を追加し、mainへマージした。`npm test`は127/127成功し、実ブラウザ・実機確認とは分けて扱う。

- Pull Request #55・#56で、Phase 8B検収の基準コミットと未確認理由を文書へ同期した。ゲーム本体・自動試験コードは変更していない。
- Pull Request #58・#59で、低い画面更新頻度でも30秒締切の残stepを小分け精算し、RESULTへ進む経路をmainへ統合した。PR #59配信後のCloud Chromeで入力なしの公式30秒プレイがRESULTへ進むことを確認した。

### 2.2 未完了

- 2026-08-09の敵対的検証で、有限面を通らない反射得点、判定波の表示切り捨て、6回未満の入力拒否、寿命後得点、ビーコン速度暴走を再現した。G4を再開・不合格へ戻し、[`PHYSICS_SCORE_INTEGRITY_RECOVERY_PLAN_v3.md`](PHYSICS_SCORE_INTEGRITY_RECOVERY_PLAN_v3.md)に従って回復する。
- 同じ時刻付き入力でも20/30Hzと60/120Hzで得点が異なり、締切をまたぐ長いフレームで締切前の物理stepを失った。まとまりBをPull Request #43で統合後、独立検証で見つかった締切確定と画面休止境界を別の補修提案で直し、Nodeの再現試験を通過した。
- G4、時刻処理、描画復旧が再合格するまで、Phase 5B・5C、ランキング、競技版公開を停止する。回復CはPull Request #45でmainへ統合済みだが、実ブラウザと実機確認は未完了である。
- Phase 4Aの反射親子関係、有限線分判定、端点距離、面履歴、寿命継承、エネルギー減衰はPull Request #39でmainへ統合済みである。旧18論理ピクセル再判定距離は、今回の同一面再使用禁止で置き換える。
- Phase 4Cの固定更新、単調増加時計、画面休止時の停止と復帰はPull Request #40でmainへ統合済みである。iPhone実機の休止復帰は未確認である。
- 最高候補方式と、入力時刻を固定step境界へ適用した同一列の再現性は自動試験済みである。iPhone実機の休止復帰、ランキング再開前のサーバー条件は未完了である。
- 時刻fixtureを混ぜた20/30/60/120Hz、可視中の50/100/150/1000msの長い描画間隔、締切跨ぎで反射系列・得点・内訳・wave ID・ビーコン位置が一致する。回復Cの有限反射弧、WebGL復旧、Canvas切替はローカル自動試験済みだが、実ブラウザ強制消失と実機休止復帰は未確認である。
- Phase 5Aの読み取り専用監査で、正式Project URL、既存記録0件、RPC、RLS、直接INSERT権限を確認した。コードのURLは後続修正で正式URLへ一致した。`public.games`の`namioshi`未登録、v3と旧版を分ける現行RPCの条件不足は未解決である。
- Pull Request #46でPublishable keyをBearerへ入れないクライアント契約へ修正済み。サーバー契約と実通信は未確認である。
- Pull Request #49で結果画面の端末ベスト、回数、保存状態、内訳、終了導線を追加済み。実機確認は未完了である。
- Phase 6Bでsafe-area、横画面、アクセシビリティのコード契約を追加した。iPhone・iPad実機とVoiceOverの確認は未完了である。
- WebGL描画の毎フレーム割り当てとlocation検索はPull Request #51で補修済み。実ブラウザとiPhone性能は未確認である。
- Phase 7Bの波帯、水面、ガラス、ビーコン、命中表示はPull Request #52でmainへ統合済みである。実ブラウザと実機での視認性・性能は未確認である。
- Phase 7Cでは、既存の効果音を維持したまま、対応環境だけで使える任意の振動通知を追加し、Pull Request #53でmainへ統合した。振動は初期設定で無効、非対応環境ではゲームを止めない。実ブラウザと実機の振動は未確認である。
- Phase 8Aでは、Pull Request #54で共有経路と画面導線の不足していた自動契約を追加し、mainへ統合した。GitHub Actions Run #91でNode.js 18・20・22が成功した。Chromium/WebKitの実行バイナリがないため、実ブラウザ自動試験は未実施である。
- Phase 8Bでは、実ブラウザ・実機検収の記録欄と未確認範囲を[`PHASE8B_DEVICE_TEST_REPORT_v3.md`](PHASE8B_DEVICE_TEST_REPORT_v3.md)へ整理する。実機結果がないため、G4・G7・G8、ランキング再開、公開判定は未完了である。
- WebGLコンテキスト消失のローカル復旧契約は回復Cで実装した。実ブラウザ強制消失とiPhone性能は未確認である。
- 全判定波の前景表示は回復計画のまとまりAで自動試験済み。最大波場面のiPhone性能は未確認である。
- iPhoneの初回プレイ確認は問題なしの報告を受けたが、長時間・反復・発熱を含む検収は未確認である。
- iPad、Codeberg Pages、実Supabase通信を確認していない。

### 2.2.1 2026-08-12 公開版Cloud Chromeの後続確認

- 公開版をCloud Chromeで実行し、PLAYINGから締切をまたいだ後も「残り 0.0」のまま35秒以上RESULTへ遷移しない失敗を確認した。
- ゲーム本体URLのコンソールエラーは確認されなかった。実機、iPhone・iPad、WebGL強制消失・復帰、Canvas 2D切替、音・振動、長時間動作の確認済みとは扱わない。
- rendererSuspended中に締切決済を継続する最小修正と、自動回帰契約をDraft候補へ追加する。ゲームルール、得点式、ランキング、Supabase設定は変更しない。

### 2.2.2 2026-08-13 Pull Request #58統合後の再失敗と第2回復

- PR #58統合後のmain `f10a77159eafed3dce0b347332723a75ac46a882` がGitHub Pagesへ反映されていることを公開JavaScriptで確認した。
- Cloud Chromeで公式モードを再実行し、締切後32秒以上「残り 0.0」のままRESULTへ遷移しない失敗を再現した。ページは可視状態で、ゲーム本体由来のコンソールエラーは確認されなかった。
- 1Hz相当では30秒時点で90/1800固定step、未処理1710stepとなり、従来の1画面3stepだけでは追加570画面更新を要する。PR #58のrendererSuspended補修だけでは通常経路の低い画面更新頻度を解消できなかった。
- 第2回復では、締切後だけ最大60論理フレーム・180固定stepずつ処理し、各処理の間でブラウザへ制御を返す。締切前の1画面最大3step、入力順、得点、30秒、6タップは変更しない。
- 1Hz fixtureは最大10回の小分け処理で1800stepへ到達し、既存fixtureと同じ5481点・内訳を維持することを完了条件へ追加する。
- 自動試験の成功は、Draft反映後のCloud Chrome、main配信後、iPhone・iPad、WebGL消失・復帰、Canvas 2D、長時間動作の合格へ置き換えない。

### 2.2.3 2026-08-13 Pull Request #59統合後・戦略ルール候補

- PR #59統合mainは`4689107`。ActionsとPages配信成功後のCloud Chromeで、入力なしの公式30秒プレイがRESULTへ進んだ。
- 次のDraftは波速110と二段階台帳を同じ競技ルール版`namioshi-v3-strategy-002`で扱う。30秒、6入力、寿命3秒、得点基礎値、配置ID・指紋は維持する。
- 第一段階は各`rootTapId × beaconId`の最高候補、第二段階は確定済み候補の`beaconId × 順序付き経路`最高1件とする。完全な経路キー置換は行わない。
- ランダムと設計入力を同じ1/6/11/16/21/26秒へそろえ、seed 1〜1000と独立holdout seed 10001〜11000を固定した。配置比較時の波速165 snapshotは上書きせず、戦略分析を別snapshotへ保存する。
- 自動分析ではcalibration中央値1803、holdout中央値1881、同地点最高標本3106、2地点交互baseline最高標本3293、設計fixture4362。設計fixtureは同地点・2地点の両baseline最高標本に対して1.25倍以上をCIで要求する。設計fixture、同地点最高標本、holdout固定例は20/30/60/120Hzで一致する。
- 人が初見で経路を理解・再現できるか、候補の実ブラウザ表示、iPhone・iPad、継続性能は未確認である。

### 2.2.4 2026-08-16 PR #71統合後の現行状態

- 上記2.2および2.2.1〜2.2.3は各時点の検証・回復計画を残す履歴であり、6回・6480点・旧ルール版の記述を現行値として使わない。
- 現行mainはPR #71統合コミット`4385d14c633c0d7d39c9b8b80b5fa2c277a6ff0c`。根波は最大10回、11回目以降は無視、30秒、波速110、総hard ceiling11200点である。
- PR #71の可視反射弧共有判定について、151テスト、配布物・配置・戦略・SVG・容量・差分検査、敵対的884ケース、GitHub Actions #126を確認した。案内と描画の不一致は0件である。
- 実ブラウザ、iPhone・iPad、初見再現性、長時間・反復・発熱、ランキング再開、Supabase、本番公開、Ready化は未確認または未承認であり、G4・G7・G8・G9は完了扱いにしない。


### 2.3 未確認として扱う事項

- Supabase projectの正式URLは確認済み。ただし現行コードのURLと一致しない。
- Publishable keyとURLが同じプロジェクトのものか。
- `submit_score` の実RPC疎通。
- `public.games` の`namioshi`登録は確認済みで、該当行は0件だった。
- `namioshi`の既存`score_runs`、`game_scores`、`game_play_events`は0件だった。
- 現行ランキングRPCが`client_version`でv3と旧版を分けられるかは確認済み。`game_scores`を読むがversion条件がなく、分離できない。
- 実験場トップの登録方式。
- 詳細ランキングの登録方式。
- iPhone SE級、iPhone 11 Pro、iPhone 17 Pro、iPad Pro 2018での実動作。
- Codeberg Pages上のキャッシュ、MIME type、相対パス、通信動作。

未確認事項は、推測で「済」にしない。

---

## 3. 作業管理の基本規則

### 3.1 1つのPull Requestで扱う目的

1つのPull Requestは、1つの目的だけを扱う。次の変更を同時に行わない。

- ビルド整理とゲームルール変更
- 固定座標と得点式変更
- 得点式変更とランキング本番送信
- ランキング変更と画面全面改修
- WebGL全面改修と物理変更
- 実機検収と未関連の新機能追加

### 3.2 Git運用

- `main` へ直接pushしない。
- 最新の統合状態から作業ブランチを作る。
- Pull RequestはDraftで作る。
- タイトルへ「Draft」と書くだけでなく、GitHub上のDraft状態を確認する。
- 自動マージを設定しない。
- ユーザー確認前にマージしない。
- force pushを行わない。
- 由来不明の未コミット変更を削除、上書き、stash、resetしない。
- 生成物を更新するPull Requestでは、正本ソースと生成物の差を説明する。

### 3.3 各Pull Requestの開始条件

- 基準ブランチと基準コミットが特定されている。
- 作業開始時の未コミット差分が記録されている。
- 変更対象ファイルと変更禁止ファイルが先に列挙されている。
- 前段階の必須ゲートが通っている。
- 不明な値を推測しなくても作業できる。
- 本番データを変更する場合は、ユーザーの明示承認がある。

### 3.4 各Pull Requestの完了条件

- 目的外の差分がない。
- 変更した機能の自動検査がある。
- 既存の `build / verify / test` が通る。
- 実施できなかった確認を「成功」と書いていない。
- 実機確認が必要な項目は未確認のまま残している。
- 変更前へ戻す方法が説明されている。
- Pull Request本文に、変更、非変更、検証、未確認、次段階が書かれている。

### 3.5 作業停止条件

次の場合は、実装を進めず停止して報告する。

- 正本同士に解消できない矛盾がある。
- 正式なSupabase情報を確認できない。
- v3得点を旧ランキングと安全に分けられない。
- 公式配置を人が選んでいないのに本番配置を確定する必要がある。
- 変更禁止ファイルを触らないと目的を達成できない。
- 基準外の大きな未コミット変更がある。
- 必須検査が今回の変更と無関係な理由で失敗し、原因を切り分けられない。
- iPhoneで継続30fpsを下回るが、品質低下でも改善しない。
- 使用素材の権利を確認できない。

---

## 4. 目標構成

Phase 2完了後は、次を基本構成とする。実装時に必要な変更は認めるが、責務を混ぜない。

```text
/
  index.html
  package.json
  README.md
  CURRENT_TASK.md
  DECISION_LOG.md

  docs/
    REQUIREMENTS_v3.md
    SPEC_v3.md
    IMPLEMENTATION_PLAN_v3.md
    MASTER_COMPLETION_PLAN_v3.md
    REVIEW_CHECKLIST_v3.md
    RELEASE_REPORT_v3.md
    SUPABASE_AUDIT_v3.md
    DEVICE_TEST_REPORT_v3.md

  src/
    main.js
    config.js
    core/
      clock.js
      storage.js
      audio.js
      quality.js
    game/
      world.js
      viewport.js
      layouts.js
      waves.js
      reflection.js
      scoring.js
    render/
      webgl.js
      canvas.js
    services/
      ranking.js
      share.js
    ui/
      screens.js
      styles.css

  tests/
    world.test.js
    reflection.test.js
    scoring.test.js
    viewport.test.js
    ranking.test.js
    share.test.js

  tools/
    layout-lab.html
    analyze-layouts.js

  scripts/
    build.mjs
    verify.mjs
    report-size.mjs

  dist/
    index.html
    assets/
```

### 4.1 責務

- `game/**` は画面やWebGLを知らない。
- `render/**` は得点を決めない。
- `services/ranking.js` は公式結果を受け取って送信するだけにする。
- `ui/**` は状態表示と利用者操作を担当し、物理計算を持たない。
- `config.js` は公開URL、slug、ルール版、配置ID、品質設定を一元管理する。
- `dist` は `src` から一方向で生成し、手作業で修正しない。

---

## 5. ゲート一覧

| ゲート | 条件 | 状態 |
|---|---|---|
| G0 仕様契約 | v3正本文書と決定ログがある | 完了 |
| G1 現行UIの正しさ | 重複ID、見せかけLOADING、共有フォールバックを修正 | 完了。実機未確認 |
| G2 開発構成 | JavaScript正本、再現可能なビルド、不要依存削除、容量報告化 | 完了 |
| G3 公平な盤面 | 360×640固定座標、公式配置、練習分離、同じタップと同じ物理step列での再現性 | 時刻fixtureの20/30/60/120Hz・長frame結果は一致。iPhone実機の休止復帰は未解決 |
| G4 物理と得点 | 有限反射経路、寿命、10入力、全判定波表示、決定的な時刻処理 | 敵対的検証で再開・不合格。PR #71後のNode自動試験と可視性検査は通過、実ブラウザとiPhone実機は未確認 |
| G5 ランキング | 正式環境確認、公式だけ1回送信、旧得点分離 | G4再合格までblocked |
| G6 画面完成 | 保存、結果内訳、アクセシビリティ、全導線 | 未完了 |
| G7 表現完成 | 高品質WebGL、音、品質段階、Canvas整合 | 未完了 |
| G8 検収完成 | 自動試験、実機、Codeberg、Supabase、回帰試験 | 未完了 |
| G9 公開 | タグ、公開、登録、監視、戻し手順 | 未完了 |

前のゲートが未完了のまま、依存する後のゲートを本番へ入れない。

---

# 6. 詳細実装計画

## Phase 2A: JavaScript正本へ統一する

### 目的

実質JavaScriptである `.ts` ファイルと疑似TypeScript変換をなくし、`src` をブラウザで読める普通のJavaScript正本にする。

### 主な作業

- 実行対象の `.ts` を `.js` へ変更する。
- 相対importへ `.js` 拡張子を付ける。
- CSSをJavaScriptからimportせず、HTMLの `link` で読む。
- `src/types` の不要な実行時placeholderを削除する。
- 未使用の旧シェーダーファイルを参照確認後に削除する。
- root `index.html` を `./src/main.js` と `./src/ui/styles.css` へ向ける。
- `scripts/build.mjs` を再帰コピー式へ変える。
- build前に `dist` を削除する。
- `src` と `dist/assets` の対応ファイルをバイト単位で一致させる。
- `.ts / .tsx` が残った場合はbuildを失敗させる。

### 変更してはいけないもの

- 物理定数
- 得点式
- 配置
- UI状態
- WebGLの描画結果
- Canvas 2Dの描画結果
- Supabase設定
- 共有文

### 自動検査

- すべての `.js` へ構文検査を行う。
- すべての相対importの参照先を確認する。
- `.ts`、`import type`、型注釈、bare importを検出する。
- `src` と `dist/assets` の差を検出する。
- rootとdistのHTML参照を確認する。

### 完了条件

- `src` に `.ts / .tsx` がない。
- 疑似トランスパイルを使わない。
- 既存ゲームの条件式、数値、文字列、呼び出し順が変わっていない。
- G1の画面と共有処理が維持されている。

### 推奨ブランチ

`codex/namioshi-v3-phase2a-js-source`

---

## Phase 2B: 不要依存と旧容量制限を削除する

### 目的

実際には使っていないThree.js、Vite、TypeScriptの代替登録を削除し、再現可能で説明しやすい開発構成へする。

### 主な作業

- `vendor/three` を参照調査後に削除する。
- `vendor/vite` を参照調査後に削除する。
- `vendor/typescript` を参照調査後に削除する。
- 不要になった `vite.config.js` と `tsconfig.json` を削除する。
- `package.json` から未使用依存を削除する。
- `package-lock.json` を実態へ合わせるか、依存がなくなれば削除可否を判断する。
- `.gitignore` を整備し、`node_modules`、一時ファイル、ローカル検査結果を除外する。
- `npm run size` を容量報告専用へ変える。
- 2,900,000バイト超過だけを理由に失敗させる処理を削除する。
- source map、重複素材、未使用素材、古い生成物の検査は維持する。

### 完了条件

- `npm install` なしでも、標準Node.jsだけでbuildとverifyが可能か、必要依存が明確である。
- 偽装したVite、TypeScript、Three.jsパッケージがない。
- 容量は表示されるが、固定上限で失敗しない。
- 同一コミットから同じ `dist` を再生成できる。

### 推奨ブランチ

`codex/namioshi-v3-phase2b-build-cleanup`

### G2通過条件

Phase 2Aと2Bがともに完了し、別環境で `build / verify` を再実行できること。

---

## Phase 3A: 360×640固定論理座標を導入する

### 目的

端末の画面サイズが得点、波速度、距離、反射時刻へ影響しないようにする。

### 実装契約

```text
論理幅: 360
論理高: 640
表示倍率: min(viewWidth / 360, viewHeight / 640)
表示幅: 360 × 表示倍率
表示高: 640 × 表示倍率
左右余白: (viewWidth - 表示幅) / 2
上下余白: (viewHeight - 表示高) / 2
```

入力変換:

```text
logicalX = (clientX - canvasRect.left - offsetX) / scale
logicalY = (clientY - canvasRect.top  - offsetY) / scale
```

論理領域外の入力は拒否する。

### 主な作業

- `viewport.js` を追加する。
- Worldの幅と高さを360×640へ固定する。
- 画面サイズ変更でWorldを作り直さない。
- WebGLとCanvas 2Dの両方へ同じ表示変換を適用する。
- HUDとゲーム入力領域を分離する。
- 画面回転時にプレイ状態を壊さない。
- DPRは描画解像度だけに使い、物理座標へ使わない。

### 自動検査

- 320×568、375×812、390×844、1024×1366で同じ論理入力を与え、同じ波位置と得点になる。
- 余白上の入力が拒否される。
- 左上、中央、右下の変換誤差が0.25論理ピクセル以内である。
- 画面回転前後でWorldの状態が変わらない。

### 完了条件

- 端末サイズを変えても公式シミュレーション結果が一致する。
- ゲーム領域を切り取らない。
- 320×568で操作領域が視認できる。

### 推奨ブランチ

`codex/namioshi-v3-phase3a-logical-viewport`

---

## Phase 3B: 公式配置比較ラボを作る

### 目的

Codexが感覚だけで公式配置を決めず、複数候補を自動比較し、人が最終選択できるようにする。

### 本番へ入れない比較機能

```text
tools/layout-lab.html
tools/layout-candidates.js
tools/analyze-layouts.js
docs/OFFICIAL_LAYOUT_STUDY_v3.md
```

### 候補条件

少なくとも3候補を作る。各候補は次を固定する。

- ビーコン3個の初期位置
- ビーコン3個の基準軌道と速度
- ガラス片4個前後の位置、角度、長さ
- 公式配置ID
- ルール版

### 比較項目

- 直接波だけで得られる最大候補得点
- 壁反射が有効な場所の数
- ガラス反射が有効な場所の数
- 2回反射が理論上可能か
- ビーコンとガラスの視覚重なり
- 6タップの選択肢が一つに固定されすぎないか
- 運ではなく再現可能な攻略があるか
- 320×568で要素を識別できるか

### 人の承認が必要な項目

- 採用する公式配置
- 難しさ
- 直接波の価値
- 反射の気持ちよさ
- 6回の入力が十分か

Codexは候補の数値を出せるが、最終的な遊びやすさを勝手に決めない。

### 完了条件

- 3候補以上の比較結果が同じ形式で記録されている。
- 本番コードを変更していない。
- ユーザーが採用候補を明示している。

### 推奨ブランチ

`codex/namioshi-v3-phase3b-layout-lab`

---

## Phase 3C: 公式モードと練習モードを分ける

### 目的

ランキングの公平性を確保しつつ、ランダム配置でも自由に遊べるようにする。

### 公式モード

- 採用済み公式配置を使う。
- `Math.random()` を使わない。
- 公式配置IDとルール版を持つ。
- ランキング送信候補になる。

### 練習モード

- ランダム配置を許可する。
- ランキング送信を行わない。
- RESULTへ「練習結果」と表示する。
- ベスト記録は公式と分ける。

### 完了条件

- モードの状態がWorldへ明示的に渡される。
- 練習結果からランキング送信関数を呼べない。
- 同じ公式配置IDで初期状態が一致する。
- ランダム関数を差し替えた試験が可能である。

### 推奨ブランチ

`codex/namioshi-v3-phase3c-official-practice`

### G3通過条件

固定論理座標、採用済み公式配置、公式と練習の分離が揃い、同じタップと同じ物理step列で同じ結果になること。

実描画間隔が60Hz相当と120Hz相当で異なる場合の結果一致はG3に含めず、Phase 4Cの固定更新とG4の通過条件で扱う。

---

## Phase 4A: 波の親子関係と反射処理を作り直す

### 目的

反射元で即座に再反射する問題、親子波の追跡不能、同じ物理現象の重複処理を解消する。

### 波データ

各波は最低限、次を持つ。

```js
{
  waveId,
  rootTapId,
  parentWaveId,
  originX,
  originY,
  previousRadius,
  radius,
  width,
  speed,
  age,
  lifetime,
  energy,
  reflectionDepth,
  reflectedBy,
  surfaceHistory,
  hitCandidates
}
```

### 反射規則

- 反射深度は0、1、2のいずれかである。
- 深度2の波は新しい反射波を作らない。
- 壁反射では仮想発生源を壁の反対側へ鏡写しする。
- 壁とガラスは有限線分として扱う。
- ガラス反射では、有限線分上の接触点を基準に発生源を鏡写しする。
- 線分外へ射影される場合は、近い端点までの実距離で接触を判定する。
- 線分の延長線だけに近く、端点へ到達していない波を反射させない。
- 反射子波は、親が衝突した時点の総移動距離を引き継ぐ。
- 子波を半径1から再開しない。
- `previousRadius` と `radius` の間で面を横切った時だけ反射を発生させる。
- 親と子へ反射元の面IDを記録する。
- 同じ波から同じ面への子波は1回だけ作り、同じ祖先経路で同一面を再使用しない。
- 同じ波と同じ面の組み合わせは1回だけ反射する。
- Worldを同じ条件でresetした時は、波IDと反射ログも同じ順序から再現する。
- 子波の年齢と寿命は親の経過を引き継ぎ、反射のたびに寿命を最初からやり直さない。
- 波配列の走査中に追加・上限削除しても、未処理波を飛ばさない。

### Phase 4A 当時の実装値と現在の置き換え

- 旧18論理ピクセル再判定距離は廃止し、同じ波から同じ面への送出1回、同じ祖先経路での同一面再使用禁止へ置き換える。
- 反射子波は親波の現在半径、年齢、寿命を引き継ぐ。
- 壁反射のエネルギー係数は0.72、反射板反射は0.84とする。
- 有限線分の射影が外れた場合は、端点までの実距離で判定し、端点へ届く前の延長線反射を拒否する。
- 旧24波上限は廃止し、壁4面・反射板4面・深度2・根波6個から導く構造上限390波へ置き換える。

### エネルギー

```text
根の波: 1.0
壁反射: 親energy × 0.72
ガラス反射: 親energy × 0.84
```

エネルギーは見た目へ使い、当たり判定中心をずらさない。

### 自動検査

- 左、右、上、下の壁で1回だけ反射する。
- 反射直後に同じ壁で2回目が発生しない。
- ガラス片の端点へ実距離で到達するまで反射せず、端点付近で重複反射しない。
- ガラス片の延長線上だが有限線分から離れた波は反射しない。
- 深度が2を超えない。
- 親子で `rootTapId` が一致する。
- 子の `parentWaveId` が正しい。
- Worldをresetして同じ入力を与えると、波IDと反射ログが一致する。
- 同じタップと同じ物理step列で、正規化した反射イベント系列が一致する。

### 完了条件

- 反射経路をログで追跡できる。
- 同じタップと同じ物理step列で同じ反射系列になる。
- 現在の見た目を一時的に簡略化しても、判定が正しい。

### 推奨ブランチ

`codex/namioshi-v3-phase4a-wave-reflection`

---

## Phase 4B: 最高候補得点方式を実装する

### 目的

同じタップ由来の複数波が同じビーコンへ何度も加点する問題をなくし、反射を狙う意味を明確にする。

### 基礎点

| 種類 | 基礎点 |
|---|---:|
| 直接波 | 20 |
| 壁1回反射 | 100 |
| ガラス1回反射 | 180 |
| 2回反射 | 300 |

### 命中精度

```text
accuracy = clamp(1 - error / hitBand, 0, 1)
candidateScore = round(baseScore × (0.8 + accuracy × 0.4))
```

1候補の最大は360点である。

### 加点台帳

`rootTapId × beaconId` の6×3台帳を持つ。

```js
bestCandidates[rootTapId][beaconId] = {
  score,
  type,
  error,
  waveId,
  reflectionDepth
}
```

新しい候補が高い時だけ、差額をプレイ中スコアへ加える。

結果画面の内訳は、最終的に採用された9枠の候補から再集計する。低い候補から高い候補へ更新された場合、古い種類を最終内訳へ残さない。

同点時の表示分類は、実装前に次の順で固定する。

1. 候補得点が高い
2. 反射深度が高い
3. 命中誤差が小さい
4. `waveId` が小さい

この順序は合計得点を変えず、結果内訳を決定的にするために使う。

### 全体コンボ

公式モードでは全体コンボ倍率を削除する。練習モードでも同じ得点式を基本とし、別の演出倍率を付ける場合は得点へ加えない。

### 理論上限

暫定上限は次である。

```text
6 root taps × 3 beacons × 360 = 6480
```

実装後、列挙試験と公式配置試験で確定する。理論上限を変更する場合は、実装前に正本文書を更新する。

### 完了条件

- 同じ根の波とビーコンで加点枠が1つだけである。
- 候補更新時は差額だけが増える。
- 最終内訳の合計が最終スコアと一致する。
- 公式スコアが6480を超えない。
- 旧v2ランキングへまだ送信しない。

### 推奨ブランチ

`codex/namioshi-v3-phase4b-scoring-ledger`

---

## Phase 4C: 固定更新と画面休止を実装する

### 目的

画面更新速度、バックグラウンド、端末負荷によって物理結果と30秒計測が変わらないようにする。

### 固定更新（Pull Request #40の旧契約から、回復まとまりBで更新済み）

```text
物理刻み: 1 / 60秒
1描画で進める最大回数: 3
可視中の未処理時間: 捨てずに後続描画へ持ち越す
締切跨ぎ: 30秒までを供給し、同じ締切時刻で残stepを排出する
```

まとまりBでは、1描画で進める最大3stepを維持しつつ未処理stepを捨てず、入力を時刻順に適用し、30秒締切までの残stepと保留中の命中候補を精算してからRESULTへ進める。画面非表示前の可視時間と入力は保持し、非表示時間そのものは物理へ加えない。休止移動後も締切未満に残る受理済み入力は、物理非進行の締切終端で一度だけ適用する。

ゲーム終了は、単純なフレーム減算だけでなく、プレイ開始時の単調増加時計を基準にする。

6タップを使い切ったことや、表示中の波が0件になったことを早期終了条件にしない。通常のプレイは30秒の締切までPLAYINGを維持する。

### 画面非表示

PLAYING中に非表示になった場合:

- 描画と音を停止する。
- 30秒の締切は進める。
- 非表示前までの可視accumulatorと保留入力を保持する。
- 未超過なら、境界と入力時刻を非表示時間分だけ後ろへ移して固定更新を再開する。
- 締切精算を開始済みなら残stepを保持し、復帰後に同じ締切時刻で精算してからRESULTへ進む。
- 精算開始前のまま締切を超えた場合も、非表示前の可視accumulatorを同じ締切時刻で精算してから、保留命中を確定してRESULTへ進む。境界と入力時刻は非表示開始から締切までの分だけ移し、締切以上になった入力は破棄する。
- `visibilitychange`と`pageshow`が同じ復帰を通知しても時刻を二重移動せず、復帰通知内では物理stepを排出しない。

COUNTDOWN中に非表示になった場合:

- カウントダウンを取り消す。
- HOMEへ戻す。

### 完了条件

- 60Hzと120Hzで同じ論理結果になる。
- 6タップ直後に波がなくなっても、30秒未満でRESULTへ進まない。
- 可視中に1秒の長い描画間隔があっても、1描画で4step以上を一気に進めない。
- バックグラウンドで30秒の締切を超えた場合、復帰時に終了する。
- タイマーが再挑戦ごとに増えない。

### 推奨ブランチ

`codex/namioshi-v3-phase4c-fixed-step`

### G4通過条件

有限反射経路、寿命、6入力、全判定波表示、得点台帳を自動試験し、時刻付き入力を混ぜた20/30/60/120Hz、長いフレーム、締切跨ぎで得点、内訳、反射系列、波ID、ビーコン位置が一致すること。iPhone短期確認が終わるまで通過扱いにしない。

---

## Phase 5A: Supabaseと旧ランキングを監査する

### 目的

不明な接続情報を推測せず、v3ランキングを安全に設計するための事実を集める。

### このPhaseで本番スコアを送らない

調査用の架空スコアを本番ランキングへ送らない。読み取りまたは安全な接続確認だけを行う。書き込みが不可避なら、ユーザー承認後に専用の非本番slugまたはテスト環境を使う。

### 確認項目

- 正式Project URL
- Publishable keyの発行元
- `submit_score` RPCの引数と戻り値
- `public.games` の `namioshi` 登録
- `score_order / score_unit / score_scale / score_decimals / score_label / topRanking`
- v2以前のスコア件数
- `client_version` の保存有無
- 詳細ランキングが `client_version` や期間で絞れるか
- 表ごとのアクセス制御
- 直接INSERTが拒否されるか
- secret keyとservice_role keyが公開物にないか

### 成果物

`docs/SUPABASE_AUDIT_v3.md`

実値のPublishable keyは文書へ書かない。確認結果は、マスクしたproject-ref、RPC名、列名、件数、可否だけを記録する。

### 分離判断

旧スコアが存在する場合、次のいずれかをユーザーが承認するまでv3送信を有効にしない。

1. 詳細ランキングを `client_version` でv3だけに絞る。
2. 旧スコアを別期間または別シーズンとして表示する。
3. 旧スコアを保全してからアーカイブする。
4. `game_slug` を分ける。ただし、現在の決定ログを変更する正式承認が必要である。

### 推奨ブランチ

`codex/namioshi-v3-phase5a-ranking-audit`

---

## Phase 5B: ランキング送信クライアントを実装する

### 目的

公式結果だけを、共通契約で、1回だけ、安全に送る。

### 送信契約

```text
POST {SUPABASE_URL}/rest/v1/rpc/submit_score
Content-Type: application/json
apikey: {SUPABASE_PUBLISHABLE_KEY}
```

`Authorization: Bearer` は付けない。

本文:

```json
{
  "p_display_name": "プレイヤー名",
  "p_game_slug": "namioshi",
  "p_score": 1230,
  "p_client_version": "namioshi-web-3.0.0-official001"
}
```

### 送信制御

- RESULTを先に表示する。
- 公式モードだけ送る。
- `playId` ごとに送信済みフラグを持つ。
- `AbortController` で約10秒の上限を設ける。
- 古い `playId` の成功・失敗表示を無視する。
- 再挑戦、シェア、終了ボタンを通信中も有効にする。
- 応答本文、キー、内部エラー全文を画面とconsoleへ出さない。
- 練習モードでは通信関数を呼ばない。

### 自動試験

- 成功
- HTTP 4xx
- HTTP 5xx
- タイムアウト
- オフライン
- 二重クリック
- RESULT直後の再挑戦
- 古い応答の到着
- 練習モード

### 完了条件

- 1プレイ1送信を試験で確認する。
- 公式だけ送信する。
- Bearerが存在しない。
- 送信失敗でゲーム結果を失わない。

### 推奨ブランチ

`codex/namioshi-v3-phase5b-ranking-client`

---

## Phase 5C: サーバー検査とランキング移行を確定する

### 目的

ブラウザを書き換えた異常スコアを最低限拒否し、v3ランキングの公開方法を確定する。

### サーバー側で検査する候補

- `p_game_slug = 'namioshi'`
- 名前が空ではない
- 名前が許容長以内
- スコアが整数
- スコアが0以上
- スコアが確定理論上限以下
- 許可された `client_version`
- 想定外の直接INSERTを拒否

SQL変更が必要な場合は、SQL案を別ファイルで提示し、ユーザー承認前に実行しない。

### 完了条件

- v3の表示対象と旧記録の扱いが決まっている。
- 共通ランキングを壊さない。
- 本番RPCの実疎通を確認している。
- 実験場トップと詳細ランキングの登録条件が決まっている。

### 推奨ブランチ

`codex/namioshi-v3-phase5c-ranking-server`

### G5通過条件

公式結果が1回だけ送られ、旧記録とv3記録の表示方法が明確であること。

---

## Phase 6A: 端末内保存と結果画面を完成させる

### 目的

通信成否に関係なく、プレイヤーが結果を理解し、再挑戦できるようにする。

### 保存する値

```text
namioshi.displayName
namioshi.bestScore
namioshi.playCount
namioshi.settings.sound
namioshi.lastClientVersion
```

未送信スコアの自動再送信キューは作らない。

### RESULT

- 最終スコア
- NEW BESTまたは端末ベスト
- プレイ回数
- DIRECTの最終採用得点
- WALLの最終採用得点
- GLASSの最終採用得点
- DOUBLEの最終採用得点
- ランキング送信状態
- もう一度
- 結果をシェア
- 詳細ランキング
- ゲーム終了
- 実験場へ戻る

内訳合計が最終スコアと一致しない場合は検査を失敗させる。

### 完了条件

- localStorageが利用できなくてもゲームを継続できる。
- 保存できた場合だけ保存済みの表現を使う。
- 公式ベストと練習結果を混ぜない。
- 通信中も主要ボタンが使える。

### 推奨ブランチ

`codex/namioshi-v3-phase6a-result-storage`

---

## Phase 6B: 画面、スマホ操作、アクセシビリティを完成させる

### 目的

小さいiPhone、大きいiPad、キーボード、safe-area、支援機能で破綻しない画面にする。

### 画面契約

- `100dvh` を基本にする。
- `overscroll-behavior: none` を使う。
- ゲーム面は `touch-action: none` とする。
- ボタン、リンク、入力欄は必要な標準操作を残す。
- 画面パネルは縦方向へ安全にスクロールできる。
- 上下のsafe-areaを考慮する。
- 320×568で主要操作を使える。
- 横画面でゲーム領域を切り取らない。
- 名前入力へ明示的なlabelを付ける。
- ランキング名が公開されることを説明する。
- エラーは `role="alert"`、状態は `aria-live="polite"` を使う。
- `focus-visible` を用意する。
- `prefers-reduced-motion` へ対応する。
- 音のON/OFFをHOMEに置く。

### 自動・手動確認

- 320×568
- 375×812
- 390×844
- 1024×1366縦
- 1366×1024横
- 名前入力中
- 文字拡大200%
- reduced motion
- キーボード操作
- VoiceOverは実機で確認

### 完了条件

- ボタンが重ならない。
- 横スクロールがない。
- RESULTの最後のボタンまで到達できる。
- iPhone Safariでキーボードが閉じることを実機確認する。

### 推奨ブランチ

`codex/namioshi-v3-phase6b-responsive-accessibility`

### G6通過条件

HOMEからRESULTまで、通信失敗と保存失敗を含めて操作不能にならないこと。

---

## Phase 7A: WebGL描画の土台を最適化する

### 目的

見た目を追加する前に、毎フレームの不要な生成と検索をなくし、iPhoneで安定する土台を作る。

### 主な作業

- 背景用頂点配列を初期化時に確保する。
- 波uniform用配列を初期化時に確保する。
- 単位円のsin/cosを事前計算する。
- `slice()` を使わず、上限まで添字で読む。
- attribute位置とuniform位置を初期化時に保存する。
- 動的頂点バッファを使い回す。
- 描画品質ごとの波数と粒子数を実際に適用する。
- HOMEとRESULTで不要な60fps描画を避ける。
- 同じプレイ中に品質を上下させない。

### 品質段階

```text
HIGH: DPR上限1.5、表示波12、粒子90
MID:  DPR上限1.25、表示波8、粒子60
LOW:  DPR上限1.0、表示波5、粒子30
```

数値を変更する場合は実機測定を根拠にする。

### 性能検査

- warm-up後の描画経路で毎フレーム新規TypedArrayを作らない。
- 毎フレーム `getAttribLocation` と `getUniformLocation` を呼ばない。
- 10回再挑戦後にGPUバッファ数が増えない。
- MIDで継続30fpsを下回らない。

### 完了条件

- 見た目を大きく変えず、測定可能な土台ができている。
- WebGLとCanvas 2Dの判定結果が同じである。

### 推奨ブランチ

`codex/namioshi-v3-phase7a-render-foundation`

---

## Phase 7B: 高品質な水面、波、ガラス、ビーコンを実装する

### 目的

ゲームの情報を読み取りやすくしながら、暗い水面を上から斜めに見た2.5D表現を作る。

### 水面

- 青黒を基調にする。
- 中央をわずかに明るくする。
- 画面端を暗くする。
- 細かい常時波を2層まで使う。
- 波の高さから簡易的な水面傾きを作り、固定光源を反射させる。
- 重い全面後処理を必須にしない。

### 波

`gl.lineWidth` へ依存せず、内周と外周を持つ帯を `TRIANGLE_STRIP` で描く。

| 種類 | 色 | 形 |
|---|---|---|
| DIRECT | 水色 | 細い、残像少なめ |
| WALL | 青紫 | DIRECTより太く、残像あり |
| GLASS | 紫 | 白または虹色の短い縁 |
| DOUBLE | 金色 | 最も太く、強い発光 |

色だけで区別せず、太さ、残像、命中文字を組み合わせる。

### ビーコン

- 外側の柔らかい光
- 脈動する中間リング
- 白い中心
- 命中時の短い拡大
- 判定中心と描画中心の一致

### ガラス片

- 半透明の紫青
- 中心線と外側の光を分ける
- 両端を細く光らせる
- 判定線分と見た目を一致させる

### 命中表示

```text
+20 DIRECT
+100 WALL
+180 GLASS
+300 DOUBLE
```

実際の候補得点を表示し、0.5秒前後で消す。候補が更新されず加点0の場合は、誤解を招く得点表示を出さない。

### 素材

固定の総容量上限は設けない。ただし次を守る。

- 初回HOMEに不要な素材を先に読まない。
- 必須素材と追加演出素材を分ける。
- 未使用、重複、権利不明の素材を公開しない。
- 外部CDNへ依存しない。
- Canvas 2D版が素材読込失敗で起動不能にならない。

### 完了条件

- 反射種類を静止画でも区別できる。
- 320×568でビーコンとガラスを見失わない。
- 見た目を上げても入力遅延と30fps下限を守る。

### 推奨ブランチ

`codex/namioshi-v3-phase7b-visual-quality`

---

## Phase 7C: 音と振動を実装する

### 目的

画面を見続けなくても、タップ、反射、命中価値、結果を区別できるようにする。

### 音

- タップ
- 壁反射
- ガラス反射
- ビーコン命中
- 2回反射命中
- 結果表示

初回ユーザー操作後にAudioContextを有効化する。音が失敗してもゲームを止めない。

### 振動

`navigator.vibrate` は対応環境だけで使い、未対応をエラーにしない。iPhone Safariで利用できる前提にしない。

### 設定

- 音のON/OFFをHOMEに置く。
- 設定を端末内へ保存する。
- 共有やランキングの音とゲーム効果音を混ぜない。
- reduced motion時は強い画面点滅を抑える。

### 完了条件

- 音なしでも情報を理解できる。
- 連続再挑戦で音が多重化しない。
- バックグラウンド中に音を出さない。

### 推奨ブランチ

`codex/namioshi-v3-phase7c-audio-feedback`

---

## Phase 7D: WebGL消失と描画切替を完成させる

### 目的

WebGLコンテキストが失われても、ゲーム全体を再読込せずに安全に復帰させる。

### 処理

`webglcontextlost`:

- `preventDefault()` を呼ぶ。
- 入力を一時停止する。
- ゲーム状態、スコア、残り時間を保持する。
- 描画ループのWebGL呼び出しを停止する。

`webglcontextrestored`:

- シェーダー、プログラム、バッファ、テクスチャを作り直す。
- 復帰に失敗したらCanvas 2Dへ切り替える。

Canvas 2D切替:

- 同じcanvasでWebGL取得後に2D contextを取れない場合を考慮する。
- 必要なら新しいcanvasへ差し替える。
- UIとWorldは維持する。

### 完了条件

- 強制コンテキスト消失試験で復帰またはCanvas切替が成功する。
- スコアと入力回数が失われない。
- イベント登録が重複しない。

### 推奨ブランチ

`codex/namioshi-v3-phase7d-context-recovery`

### G7通過条件

MID品質で情報を明確に読み取れ、iPhone実機で性能目標を満たし、Canvas 2Dでも遊べること。

---

## Phase 8A: 自動試験を完成させる

### 目的

公開前の手作業だけに依存せず、ルール、画面、ランキング、生成物の回帰を検出する。

### 単体試験

Node.jsの標準テスト機能または同等の軽い仕組みを使う。

必須:

- 7回目のタップを拒否する。
- 1入力で1つのrootTapIdを作る。
- 反射深度が2を超えない。
- 反射元で即時再反射しない。
- 同じrootTapIdとbeaconIdで最終候補が1件になる。
- 候補更新時に差額だけ増える。
- 内訳合計と総得点が一致する。
- 理論上限を超えない。
- 同じ公式配置と入力で同じ結果になる。
- 画面サイズが違っても同じ結果になる。

### 結合試験

- 状態遷移
- カウントダウン二重起動防止
- localStorage利用可・不可
- 共有6ケース
- ランキング成功、失敗、タイムアウト、二重実行、古い応答
- 練習モードで送信しない
- WebGL初期化失敗時のCanvas 2D切替

### ブラウザ自動試験

開発時だけ使うブラウザ操作試験を導入してよい。公開物へ試験依存を入れない。

- Chromium
- WebKit
- 320×568
- 390×844
- 1024×1366
- HOMEからRESULT
- RULES開閉
- 6タップ制限
- シェアURL
- 結果内訳
- 通信失敗

デスクトップWebKitの合格をiPhone Safari実機確認の代わりにしない。

### 完了条件

- `npm test` または明確な同等コマンドで試験できる。
- `npm run build` と `npm run verify` が試験と矛盾しない。
- 失敗時にどの契約が壊れたか分かる。

### 推奨ブランチ

`codex/namioshi-v3-phase8a-automated-tests`

---

## Phase 8B: 実機検収を行う

> 現在状態: Pull Request #57の統合後。検収記録は[`PHASE8B_DEVICE_TEST_REPORT_v3.md`](PHASE8B_DEVICE_TEST_REPORT_v3.md)で管理し、実機・実ブラウザの未確認項目を合格扱いにしない。検収対象コミットは `cc9d00e8542a1d1edc34cc5731e69e03b0d2ee9e` である。Cloud Chromeでは締切後のRESULT遷移失敗を確認し、修正候補をDraftで検証中である。

### 目的

自動試験で代替できないスマホ操作、Safari、画面、熱、音、通信を確認する。

### 端末候補

- iPhone SE級 320×568
- iPhone 11 Pro
- iPhone 17 Pro
- iPad Pro 2018 縦
- iPad Pro 2018 横
- PCの主要なモダンブラウザ

利用できない端末は「未確認」とし、実在しない確認結果を作らない。

### 各端末で確認する流れ

1. 初回アクセス
2. HOME表示
3. 名前入力
4. RULES開閉
5. キーボードを閉じてCOUNTDOWN
6. 3、2、1、START
7. 6回入力し、7回目だけ拒否されることを確認
8. 直接、壁、ガラス、2回反射の視認
9. RESULT
10. 再挑戦
11. シェア
12. オフライン送信失敗
13. オンライン復帰
14. 画面回転
15. バックグラウンド復帰

### 継続試験

- 連続再挑戦10回
- 30分連続稼働
- バックグラウンド復帰10回
- 低電力モード
- 通常より20%多い表示波と粒子を使う試験
- 同じ公開候補版で3回連続合格

### 記録

`docs/DEVICE_TEST_REPORT_v3.md` に次を記録する。

- 端末
- OS
- ブラウザ
- 公開候補コミット
- 日時
- 描画方式
- 品質段階
- 成功項目
- 失敗項目
- 再現手順
- スクリーンショットまたは動画の参照

### 完了条件

- 必須端末候補の扱いが明確である。
- 継続30fps未満がない。
- 操作不能、見切れ、二重送信、音の多重化がない。
- 残る不具合は別Pull Requestで修正し、検収Pull Requestへ混ぜない。

### 推奨ブランチ

`docs/namioshi-v3-phase8b-device-report`

---

## Phase 8C: リリース候補を固定する

### 目的

公開する1つのコミットを固定し、公開前の最終確認を同じ内容で繰り返す。

### リリース候補

- バージョン: `v3.0.0-rc.1`
- `CLIENT_VERSION`: 公式配置IDとルール版を含む。
- 公式配置IDを変更しない。
- 得点式を変更しない。
- Supabase設定を変更しない。
- `dist` をクリーン環境で再生成する。

### 最終確認

- build
- verify
- unit tests
- browser tests
- size report
- secret scan
- asset rights
- Codeberg Pages previewまたは同等の静的配信
- 実Supabase疎通
- 実験場リンク
- 詳細ランキング
- シェアURL
- ロールバック

同じリリース候補コミットで3回連続して主要検収を通す。

### 成果物

- `docs/RELEASE_REPORT_v3.md`
- 生成物一覧
- 実測結果
- 未確認がないことの確認
- 既知の制限
- 戻し先タグ

### 推奨ブランチ

`release/namioshi-v3-rc1`

---

## Phase 8D: Codeberg Pagesへ公開する

### 目的

承認済みリリース候補を本番へ反映し、リンク、ランキング、共有、戻し手順を確認する。

### 公開前

- GitHubとCodebergのどちらをコード正本にするか明記する。
- 二方向で別々に編集しない。
- 公開先の既存版をタグまたはコミットで特定する。
- 本番のSupabase設定を再確認する。
- 公開物へsecretがないことを確認する。

### 公開後の即時確認

- HOMEが表示される。
- CSS、JavaScript、画像、音声の404がない。
- MIME typeエラーがない。
- WebGL版が起動する。
- Canvas 2D切替が起動する。
- 30秒でRESULTへ進む。
- 公式結果が1回だけ送信される。
- 詳細ランキングへ反映される。
- 実験場トップから開ける。
- シェアURLから開ける。

### 監視

公開後、少なくとも次を確認する。

- 初回プレイ
- 10回連続プレイ
- ランキング送信成功
- ランキング送信失敗時の画面
- iPhone Safari
- キャッシュ更新
- 古いJSと新しいHTMLの混在がないか

### 戻し条件

次のどれかがあれば、原因調査より先に前版へ戻す。

- HOMEが開かない。
- ゲームを開始できない。
- iPhoneで継続30fpsを下回り操作が難しい。
- 得点が理論上限を超える。
- 公式結果が二重送信される。
- secret keyまたはservice_role keyが公開された。
- ランキングを大量に汚す。
- 主要ボタンが使えない。

### 完了条件

- 本番URLで必須検収を通す。
- `v3.0.0` タグを作る。
- `RELEASE_REPORT_v3.md` を確定する。
- 旧版へ戻す手順を実際に確認する。

### G8・G9通過条件

自動検査、実機検査、本番確認、ランキング確認、戻し手順が揃い、必須の未確認項目が残っていないこと。

---

# 7. 承認が必要な判断

次はCodexだけで決めない。

| 判断 | 承認時期 | 承認者が確認する内容 |
|---|---|---|
| 公式配置 | Phase 3B後 | 遊びやすさ、反射の意味、難しさ |
| 得点式の最終固定 | Phase 4B前後 | 直接波と反射波の価値、理論上限 |
| 旧ランキングの扱い | Phase 5A後 | 保全、分離、アーカイブ方法 |
| SQLの実行 | Phase 5C | 影響範囲、戻し方、本番対象 |
| 画面の最終見た目 | Phase 7B | 読みやすさ、世界観、反射種類の区別 |
| 音の採用 | Phase 7C | 音量、繰り返し疲労、消音時の理解 |
| リリース候補 | Phase 8C | 実機結果、既知の制限、戻し先 |
| 本番公開 | Phase 8D | Codeberg、ランキング、実験場、共有 |

承認がないまま次の本番依存Phaseへ進めない。

---

# 8. 品質測定の基準

## 8.1 ゲーム結果の決定性

同じ次の入力が同じ結果になること。

```text
公式配置ID
ルール版
固定物理刻み
3つの論理タップ座標
各タップの論理時刻
```

比較対象:

- 320×568
- 390×844
- 1024×1366
- 60Hz相当
- 120Hz相当

スコア、採用候補、反射系列が一致しなければ不合格とする。

## 8.2 フレーム時間

- 60fpsを目標にする。
- 平均値だけでなく、遅いフレームを確認する。
- プレイ中の50ms超を調査対象にする。
- 意図しない100ms超を公開停止条件にする。
- 継続30fps未満を公開不合格にする。

測定は、最も波と粒子が多い場面で行う。

## 8.3 入力反応

pointerdownの受理時刻から、次の描画で波が確認できるまでを測る。

- 目標: 50ms以内
- 100ms超: 不合格

音の遅延と描画の遅延を分けて記録する。

## 8.4 継続安定性

対応APIがある環境ではJS heapを記録する。iPhone Safariで詳細値を取得できない場合は、次を代替指標とする。

- イベント登録数
- タイマー数
- 波配列上限
- 粒子配列上限
- WebGLバッファ数
- AudioNodeの残留
- 10周目のフレーム時間
- 1周目と10周目の入力反応

## 8.5 読み込み

総容量には固定上限を設けない。代わりに次を記録する。

- HTML表示まで
- HOME表示まで
- 開始可能になるまで
- 必須素材の転送量
- 追加素材の転送量
- キャッシュ有無
- 404と再試行

HOMEに不要な高品質素材は、HOME表示後に読み込んでよい。

---

## 2026-08-12 Phase 7Aの進行状態

Phase 6B PR #50の統合後、Supabaseとランキングを保留したままPhase 7A「WebGL描画の土台を最適化する」を開始した。attribute・uniform位置を初期化時に保存し、背景用静的バッファと動的頂点バッファを分離し、`bufferSubData`で動的内容を更新する。単位円のsin/cos、背景波の転記、粒子数の上限を再利用可能な形へ整理した。

品質設定はHIGH（DPR 1.5・背景波12・粒子90）、MID（DPR 1.25・背景波8・粒子60）、LOW（DPR 1.0・背景波5・粒子30）とする。品質を下げても得点対象の前景波は隠さない。プレイ中の品質は固定し、HOME・RULES・COUNTDOWN・RESULT・ERRORは最大30fpsで描画する。

ローカルでは109/109テスト、build、verify、配置分析、SVG生成一致、容量報告、差分検査が成功した。実ブラウザのWebGL描画、コンテキスト消失・復帰、Canvas 2D切替、iPhone・iPadのフレーム時間・入力反応・長時間動作は未確認であり、G4、G7、ランキング、公開は未完了のまま維持する。

---

# 9. 自動検査コマンドの最終形

Phase 8までに、最低限次を用意する。

```text
npm run build
npm run verify
npm run test
npm run test:browser
npm run report:size
npm run report:assets
npm run report:performance
```

固定容量超過で失敗する `npm run size` は廃止または報告用へ改名する。

`verify` は最低限、次を検査する。

- source mapが公開物にない
- secret keyとservice_role keyがない
- 直接ランキングINSERTがない
- bare importと外部CDN依存がない
- 相対importが解決する
- srcとdistの関係が正しい
- 古い生成物がない
- HTML参照が正しい
- 未使用・重複素材がない
- 公開物のJavaScript構文が正しい

---

# 10. リスク台帳

| ID | リスク | 影響 | 対応 | 停止条件 |
|---|---|---|---|---|
| R1 | Supabase URLとkeyが別プロジェクト | ランキング不能 | Phase 5Aで同一性確認 | 一致を確認できない |
| R2 | v2得点とv3得点の混在 | 順位不公平 | 版・期間・表示を分離 | 分離手段が決まらない |
| R3 | 仮想発生源の反射重複 | 異常得点 | swept判定とsurfaceHistory | 深度・回数試験に失敗 |
| R4 | 端末ピクセル依存 | 端末ごとに難易度差 | 360×640論理座標 | 同入力結果が不一致 |
| R5 | WebGL線幅差 | 波が見えない | 帯状TRIANGLE_STRIP | iPhoneで種類判別不可 |
| R6 | 毎フレーム割り当て | 途中でカクつく | 配列・location再利用 | 継続30fps未満 |
| R7 | iOS音声制限 | 音が鳴らない | 初回操作で有効化、無音継続 | 音失敗でゲーム停止 |
| R8 | キーボードとsafe-area | ボタン見切れ | blur、dvh、スクロール | 320×568で操作不能 |
| R9 | 送信二重化 | ランキング汚染 | playId、1回制御 | 同一プレイ複数行 |
| R10 | Codebergキャッシュ混在 | 起動不能 | 版付き資源、公開確認 | HTMLとJS版不一致 |
| R11 | 素材の権利不明 | 公開撤回 | 出所記録、未確認素材不採用 | 権利確認不能 |
| R12 | Codexの目的外変更 | 回帰、確認不能 | 1目的1PR、変更禁止範囲 | 大規模差分を説明不能 |
| R13 | Draftの名目だけで即マージ | 未検収変更がmainへ入る | GitHubのdraft状態確認 | ユーザー確認前マージ |
| R14 | GitHubとCodebergの二重編集 | 正本不明 | 一方向同期を決定 | 同じ版に異なる変更 |

---

# 11. 完了までの推奨Pull Request順

| 順番 | Pull Request | 依存 | 主なゲート |
|---:|---|---|---|
| 1 | Phase 2A JavaScript正本 | G1 | G2 |
| 2 | Phase 2B ビルド整理 | Phase 2A | G2 |
| 3 | Phase 3A 固定論理座標 | G2 | G3 |
| 4 | Phase 3B 配置比較ラボ | Phase 3A | 人の配置承認 |
| 5 | Phase 3C 公式・練習分離 | 配置承認 | G3 |
| 6 | Phase 4A 反射親子関係 | G3 | G4 |
| 7 | Phase 4B 得点台帳 | Phase 4A | 得点承認 |
| 8 | Phase 4C 固定更新 | Phase 4A | G4 |
| 9 | Phase 5A Supabase監査 | G4と並行可 | 移行承認 |
| 10 | 回復A 物理・得点整合性（Pull Request #42、統合済み） | 敵対的検証 | G4再開 |
| 11 | 回復B 時刻・入力・得点再検証（Pull Request #43・#44統合済み） | 回復A | Node再現試験 |
| 12 | iPhone短期確認（Pull Requestなし） | 回復B | 端末停止条件 |
| 13 | 回復C 有効反射弧・WebGL復旧・安全なCanvas切替 | 回復B、iPhone短期確認 | G4・G7 |
| 14 | 回復D ランキング安全化 | 回復C、Phase 5A | G5 |
| 15 | Phase 5B 送信クライアント | 回復Dのサーバー契約 | G5 |
| 16 | Phase 5C サーバー検査 | 回復D、Phase 5B、ユーザー承認 | G5 |
| 17 | Phase 6A 保存・結果 | G4 | G6 |
| 18 | Phase 6B 画面・支援機能 | Phase 6A | G6 |
| 19 | Phase 7A 描画土台 | G4 | G7 |
| 20 | Phase 7B 高品質表現 | Phase 7A、G6 | 見た目承認 |
| 21 | Phase 7C 音 | Phase 6A | G7 |
| 22 | Phase 7D WebGL復帰 | Phase 7A | G7 |
| 23 | Phase 8A 自動試験 | 各機能Phaseと並行 | G8 |
| 24 | Phase 8B 実機検収 | G5、G6、G7、G8 | G8 |
| 25 | Phase 8C リリース候補 | 実機合格 | G9前 |
| 26 | Phase 8D 本番公開 | ユーザー承認 | G9 |

不具合が見つかった場合は、検収Pull Requestへ修正を混ぜず、原因ごとに別Pull Requestを作る。

回復CはPhase 7Dの復旧基準を前倒しで満たす停止解除工程であり、有限反射弧、無効接触の通知抑制、WebGL復帰、新canvasによるCanvas 2D切替を扱う。ローカルNode 91件、build/verify/配置/容量確認、Pull Request #45のGitHub Actions Run #66（Node.js 18、20、22）は成功したが、実ブラウザ強制消失、iPhone確認が残る。回復DはPhase 5B・5Cへ入る前にランキング安全化契約と拒否条件を固定する工程である。実際の送信クライアントとサーバー検査は、その契約に従ってPhase 5B、Phase 5Cで分けて実装する。

---

# 12. 最終公開チェック

次の全項目が `[済]` でなければ `v3.0.0` として公開しない。

## 製品

- 公式配置が承認済み。
- 得点式と理論上限が確定済み。
- 公式と練習が分離済み。
- 30秒、6タップ、反射2回が自動試験済み。

## ランキング

- 正式Supabase接続先を確認済み。
- 共通RPCの実疎通済み。
- 旧記録の扱いを承認済み。
- 公式だけ1回送信することを確認済み。
- 直接INSERT、Bearer、secret、service_roleがない。

## 画面

- 320×568で操作可能。
- safe-area、キーボード、横画面を確認済み。
- RESULTの全導線を確認済み。
- 共有文末URLを確認済み。

## 描画・音

- WebGLで全種類を見分けられる。
- Canvas 2Dで遊べる。
- context loss試験済み。
- 音なしでも理解できる。
- 音の多重化がない。

## 性能・継続

- 継続30fps未満なし。
- タップ反応50ms目標を測定済み。
- 10回再挑戦済み。
- 30分連続稼働済み。
- バックグラウンド復帰10回済み。
- オフラインと再接続済み。
- 同一候補版で3回連続合格済み。

## 公開

- Codeberg Pagesで起動済み。
- 実験場トップから到達済み。
- 詳細ランキングから到達済み。
- シェアURLから到達済み。
- 旧版へ戻す手順を確認済み。
- リリースタグと報告書を作成済み。

---

# 13. 次回作業

Phase 5Aの読み取り専用監査は完了したが、敵対的検証によりG4「物理と得点」を不合格へ戻した。Phase 5B・5Cへは進まず、[`PHYSICS_SCORE_INTEGRITY_RECOVERY_PLAN_v3.md`](PHYSICS_SCORE_INTEGRITY_RECOVERY_PLAN_v3.md)の順で修正する。

まとまりAはPull Request #42で、まとまりBはPull Request #43・#44でmainへ統合した。まとまりBでは、時刻付き入力列、未処理stepの保持、30秒締切までの精算、命中帯の最小誤差候補、公式配置fixtureの再検証、締切と画面休止境界の補修を実装した。次のDraftではまとまりCとして、有限反射弧、無効仮想接触の通知抑制、WebGL復旧、新canvasによるCanvas 2D切替を扱う。

まとまりBの入力区間は`[開始時刻, 開始時刻+30秒)`であり、入力は固定境界へ到達したstepの終了後に適用する。可視中の長いフレームは最大3stepずつ精算し、締切をまたぐ場合も`min(now, deadline)`までを供給してから同じ締切時刻で残りを排出する。画面非表示前の可視accumulatorと保留入力は保持し、通常復帰では境界と入力時刻を非表示時間分だけ後ろへ移す。締切精算開始後の残stepは画面が隠れても保持する。30秒までに観測した保留命中を物理を進めず確定してからRESULTへ進む。まとまりCではこの状態を保持したまま、有限反射弧とWebGL消失復旧を描画へ反映する。

公式配置candidate-c-open-harborの時刻fixture（137/1073/2249/3377/4193/5281ms、6入力）は20/30/60/120Hzおよび可視中の50/100/150/1000msの長い描画間隔で同一の得点・内訳・反射系列・wave ID・ビーコン位置になった。実到達例は5481点（直接0、壁239、反射板429、2回反射4813）、台帳18件、1800固定stepで、6480はhard ceilingとして保持し、探索値を最大値とは断定しない。締切直前fixture `(0,110,27025ms)` は確定前358点、最終589点で20/30/60/120Hzが一致する。まとまりCのNode自動試験91件、ローカル配布確認、PR #45のRun #66は成功したが、実ブラウザ強制WebGL消失、iPhone実機確認、まとまりD/Eは未完了である。

ランキング送信、Supabase本番変更、競技版公開は、A、B、Cが再合格するまで停止する。


## 2.2 回復D第一段階の進捗（2026-08-11）

Pull Request #45の統合後、ランキングをまだ有効化しないままクライアント側の安全契約へ着手した。正式Project URLとPublishable keyの対応は読み取り専用で確認した。URLは正式Project URLへ合わせ、Publishable keyの実値は記録しない。

当時のDraftでは、公式モード限定、play ID、0〜6480点の整数範囲、名前検査、重複送信防止、10秒中断、apikeyのみのヘッダー、古い結果の画面反映抑制を実装した。これは旧ルール時点の履歴であり、現行送信範囲は11200点、Supabase SQL、RPC、RLS、public.games、本番データ、ランキング有効化は変更しない。

p_play_idを含むサーバー側契約、許可version・rule version・season、異常隔離、頻度制限、入力再計算または自己申告表示は未完了である。クライアント側の検査だけでは競技性を証明できないため、サーバー提案とユーザー判断を経るまで公開ランキングを再開しない。


## 2.3 回復D第一段階の検証結果（2026-08-11）

Draft Pull Request #46を作成し、GitHub Actions Run #73でNode.js 18・20・22、npm test 96/96、verifyを成功させた。ランキングは未有効化で、Supabase SQL、RPC、RLS、public.games、本番データは変更していない。現行main.jsから送信を呼ばない既存契約も維持した。

## 2026-08-11 回復D D2進捗

回復D第一段階PR #46の統合後、サーバー側の安全契約を未適用SQLとして追加した。既存の共有4引数RPCとpublic記録を変更せず、namioshi専用のprivate記録、設定、頻度制限、7引数RPC、ランキング取得RPCを提案している。初期設定は無効かつself-reportedで、クライアントのランキング送信も無効のままである。

ローカル検証は99/99テスト、build、verify、レイアウト分析、SVG確認、サイズ報告、差分検査まで成功した。次のゲートはDraft PRのActions、読み取り専用レビュー、非本番でのSQL検証、自己申告か競技版かの判断である。Supabase本番への適用、限定疎通、ランキング表示接続は未承認かつ未実施とする。

## 2026-08-11 回復D D2.1進捗

D2のSQL案を公式RPC契約へ照合した結果、同名関数の引数違いによるオーバーロードは使用できないことが分かった。既存の4引数`public.submit_score`を保護するため、未適用SQLの7引数関数名を`public.submit_namioshi_score_v1`へ変更し、クライアント送信先、契約書、静的試験を同期する。SQL適用、非本番データベース作成、本番疎通、ランキング有効化は行わない。

## 14. 2026-08-12 Phase 6A統合後の進行判断

Supabaseの非本番検証とランキング連携は後回しにし、依存しないPhase 6B「画面・支援機能」を進める。PR #49で統合したPhase 6Aの端末内保存と結果画面を基準に、Draft `agent/phase6b-screen-accessibility`ではsafe-area、横画面、読み上げ用の状態情報、フォーカス表示、`prefers-reduced-motion`を追加する。

Phase 6Bではゲームルール、得点、物理、描画方式、端末保存、ランキングを変更しない。Supabase、ランキング送信、ランキング表示は無効のままとする。実機でのsafe-area、横画面、キーボード、VoiceOver、外部キーボード、強制WebGL消失は自動検査へ置き換えず未確認として残す。

ローカル検証は107/107テスト、build、verify、配置分析、SVG検査、サイズ報告、差分検査まで成功した。公開候補の判断、G4の再合格、ランキング再開、Supabase適用、実機確認は別工程である。

## 15. 2026-08-12 Phase 7A統合後・Phase 7B Draftの進行

Pull Request #51の統合後、Phase 7Bとして高品質な水面、波、ガラス片、ビーコン、命中表示を実装する。WebGLの波は再利用バッファ上の帯状三角形で描き、有限反射弧の可視範囲を変えない。Canvas 2Dは同じ反射弧を二重線で描き、描画方式の違いで判定情報を欠落させない。

Phase 7Bの変更範囲は描画と画面表示に限定する。ゲームルール、物理、得点、端末保存、ランキング、Supabase、WebGLコンテキスト消失の回復契約は変更しない。命中表示は実際に加点された正の差分だけを約0.5秒表示し、状態変更時に消去する。

Draftの自動検証は108/108テスト、build、verify、配置分析、SVG確認、サイズ報告まで成功している。実ブラウザの描画確認、WebGL消失・復帰、iPhone・iPadの視認性と性能は未確認であり、G7と公開判定は完了していない。

## 16. 2026-08-12 Phase 7B統合後・Phase 7C Draftの進行

Pull Request #52の統合後、Phase 7Cとして既存の効果音契約を維持しながら、任意の振動通知を追加する。振動は`navigator.vibrate`の実行時機能検出を使い、対応しない環境ではボタンを非対応として表示し、ゲームの進行を止めない。

振動設定は初期状態を「なし」とし、利用者がHOMEで「あり」へ変更した場合だけ端末へ保存する。タップ、壁反射、ガラス反射、命中判定、2回反射命中、結果表示に別のパターンを割り当てる。画面非表示とページ終了では振動を停止し、復帰時にゲームの状態、入力、得点、物理時間を変更しない。

Phase 7Cでは、ゲームルール、物理、得点、描画、端末保存の既存契約、ランキング、Supabaseを変更しない。ローカル自動試験、実ブラウザの振動、iPhone・iPadの実機対応、長時間試験、G7、公開判定は未確認または未完了として扱う。

## 17. 2026-08-12 Phase 7C統合後・Phase 8A Draftの進行

Pull Request #53の統合を確認した。Phase 7D相当のWebGL復旧は回復C（Pull Request #45）でコード契約まで実装済みだが、実ブラウザ強制消失と実機確認は未完了である。したがって重複実装は行わず、次のPhase 8Aとして自動試験の不足部分を補う。

今回のDraftでは、シェア文と公式URL、ネイティブ共有の成功・キャンセル・予期しない失敗、クリップボードのみ、共有機能なし、コピー失敗を試験する。また、HOMEからRESULTまでの状態画面、カウントダウン二重起動防止、RESULTのシェア・再挑戦・モード選択・終了導線、入力上限、WebGLからCanvas 2Dへの切替契約を固定する。

Phase 8Aの自動試験はNode.js標準テストで127件を実行する。Chromium、WebKit、Playwrightの実行環境がこの作業場にないため、ブラウザ操作試験は追加せず、実ブラウザ・実機確認済みとは扱わない。Supabase、ランキング再開、G4・G7の実機通過、公開判定は変更しない。

## 18. 2026-08-12 Phase 8B継続確認

- Pull Request #55をmainへ統合し、検収対象コミットを `bc038feba7774cc58ecb3ce7845012e98b80eb18` に更新する。
- Pull Request #55の変更は文書6ファイルだけで、ゲームルール、描画、音、振動、テストコード、ランキング、Supabaseを変更していない。
- PlaywrightのNode.jsパッケージは利用できたが、ChromiumとWebKitの実行バイナリがなく、Chromium導入も破損アーカイブで失敗した。実ブラウザ操作、画面キャプチャ、WebGL消失・復帰、性能測定は未実施である。
- PR #54と同じゲームコード・自動試験コードを再実行し、`npm test` 127/127、build、verify、配置分析、SVG、容量、差分検査が成功した。これは実ブラウザ・実機の合格へ置き換えない。
- iPhone・iPad、実ブラウザ、長時間・反復・発熱、G4・G7・G8、Supabase、ランキング再開、Phase 8C、公開判定は未完了のまま維持する。


## 19. 2026-08-12 PR #56統合後・Phase 8B基準更新

- Pull Request #56をmainへ統合し、現在のmainとPhase 8B検収対象を `2474ec9e9a4863e483465075cfaa294d696feb58` に更新する。
- PR #56の変更は文書6ファイルだけで、ゲームルール、描画、音、振動、テストコード、ランキング、Supabase設定を変更していない。
- 実ブラウザ・実機の確認結果は追加されていないため、G4・G7・G8、Phase 8C、ランキング再開、Supabase適用、Codeberg Pages公開は未完了のまま維持する。

## 20. 2026-08-13 PR #59統合後・戦略ルールDraft

- PR #59のmain `4689107`を後続作業の基準とする。公開版の入力なし30秒プレイはCloud ChromeでRESULTへ進んだが、Phase 8B全体は未完了である。
- 旧波速165では偶然の盤面全域到達が多く、波速110だけでも全ラウンド同地点入力が残る。経路抑制だけでは165のランダム得点が高止まりするため、波速110と追加の経路台帳を1つの競技ルール変更として検証する。
- 根タップ台帳を維持したまま経路台帳を追加し、新規・更新・発見済みを同時確定単位の1通知へまとめる。端末ベストと初回案内は競技ルール版ごとのキーへ分ける。
- `tools/strategy-analysis.snapshot.json`へ分析版、client/rule/layout、seed範囲、時刻、座標領域、分位点定義、calibration、holdout、境界探索、2地点baseline、複数Hz結果を固定する。設計fixtureは同地点・2地点の両baseline最高標本へ1.25倍以上を維持する。
- 自動受入後も、人の初見3〜5プレイ、実ブラウザの音なし表示、iPhone・iPad、WebGL復旧、反復性能は未確認として残す。初回案内の成功必須化と練習配置制約は別工程とする。
- ランキング、Supabase、Ready化、マージ、Phase 8C・8Dへは進めない。

## 21. 2026-08-14 PR #60統合後・案内と練習配置の後続Draft

PR #60のマージコミット `61f73be4b116761c2dbb08daba3811399e55fd01` を基準に、競技ルール変更とは独立した小さな後続Draftを進める。初回案内はガラス反射を1回確認するまで本番開始ボタンを無効にし、成功前のHOME復帰は許可する。練習モードは反射板の端点を盤内へ収め、ビーコン中心との距離を28px以上に制約し、最大96回の候補探索後も固定fallbackで同じ制約を守る。

自動受入はseed 1〜10000、`npm test` 138/138、build、verify、配置分析、SVG、容量、差分検査、Draft ActionsのNode.js 18・20・22成功とする。公式配置、波速110、得点台帳、ランキング、Supabaseは変更しない。実ブラウザ、iPhone・iPad、人の初見再現性、Ready化、マージは未確認または未承認のまま残す。

## 22. 2026-08-14 PR #61統合後・同時確定フィードバックの明確化Draft

PR #61のマージコミット`50d227e8bcbf61d5365d18b40ca574adaf92ab82`を基準に、同時確定通知の視認性だけを補修する。Worldの得点台帳、二段階選抜、代表判定、波速110、配置、案内、ランキング、Supabaseは変更しない。

同時確定は1通知のまま、新規・更新・発見済み件数、合計加点、代表の判定・反射種別を維持する。通知へ候補種別数と実際の加点種別数を別フィールドで追加し、画面では直接・壁反射・反射板・2回反射を件数付きで「経路」として表示する。代表値は集約全体の判定と誤解されないよう「代表」と明記し、加点0の既知通知では候補種別を表示する。

長い通知は`overflow-wrap:anywhere`で狭い画面でも折り返す。自動受入では複数種別の集計、1通知、総得点と経路台帳の整合、既存の複数Hz固定結果を確認する。実ブラウザ・iPhone・iPadでの読みやすさ、人の初見再現性、ランキング再開、Ready化、マージは未確認または未承認のまま残す。

## 23. 2026-08-14 PR #62統合後・結果画面の経路案内Draft

PR #62のマージコミット `1c724ccab97ab7a88bb7e1e55f5a2019a0670086`を基準に、結果画面の説明だけを追加する。確定済み経路台帳から、既存の得点・深度・誤差・波IDの比較規則で最高経路を1件選び、反射面の順序、判定語、得点を表示する。未命中なら最高経路がないことを表示する。

次の目標は、未命中ならビーコン命中、反射板未使用なら反射板、2回反射未達なら2回反射、それ以外なら別経路の探索・最高経路更新という優先順で1つだけ表示する。総得点、内訳、経路台帳、端末ベスト、ランキング、Supabase、競技ルールは変更しない。

自動受入は最高経路の確定値限定・順序保持・リセット時の空値、RESULTの2表示要素、既存全試験、build、verify、配置分析、戦略分析、SVG、容量、差分検査、Draft ActionsのNode.js 18・20・22成功とする。実ブラウザ・iPhone・iPadの読みやすさ、人の初見再現性、ランキング再開、Ready化、マージは未確認または未承認のまま残す。

## 24. 2026-08-14 PR #63統合後・反射板の意味を伝えるプレイ中の案内Draft

PR #63のマージコミット `7d527d5846e84e95fb083b1264f6266e28ca37e2`を基準に、プレイ中の説明だけを補修する。現在の案内に残る「棒」という呼び方をやめ、波・反射板・ビーコンを3行へ分ける。反射板は「光る線」、ビーコンは「白く光る点」と説明し、波の円、反射板の線、ビーコンの点を色付き記号で示す。

得点、波速110、二段階台帳、公式配置、結果画面、保存、ランキング、Supabaseは変更しない。自動受入は3項目のラベルと記号、反射板・ビーコンの役割、曖昧表現の除去、既存全試験、build、verify、配置分析、戦略分析、SVG、容量、差分検査、Draft ActionsのNode.js 18・20・22成功とする。実ブラウザ・iPhone・iPadの読みやすさ、人の初見理解、ランキング再開、Ready化、マージは未確認または未承認のまま残す。

## 25. 2026-08-15 PR #64統合後・プレイ中の目的と終了条件の明示Draft

PR #64統合後のmain `cc577a15896cf8e7f26b4fe1b102987fdb5ce03d`を基準に、プレイ中の凡例だけでは伝わりにくかった目的・得点条件・6回後の待機状態を補う。目的表示は「波をビーコンに重ねる」、得点説明は「ビーコン命中が得点で、反射板命中だけでは得点にならない」、待機表示は「6回を使い切っても30秒終了時まで結果を待つ」とする。

練習モードの入口には30秒・6タップ・反射経路の練習・ランキング外を表示する。表示だけの変更とし、得点式、波速110、物理、二段階台帳、30秒終了契約、公式・練習の保存分離、ランキングとSupabase無効を維持する。自動受入は目的・待機表示、RULESの基準点と得点条件、練習入口文、既存全試験、build、verify、配置分析、戦略分析、SVG、容量、差分検査、Draft ActionsのNode.js 18・20・22成功とする。実ブラウザ、iPhone・iPad、狭い画面での読みやすさ、人の初見理解、Ready化、マージは未確認または未承認のまま残す。

## 26. 2026-08-15 PR #65統合後・命中確認と得点確定の区別Draft

PR #65統合後のmain `550b09b8d1e04124001d8cf3812c218854a11441`を基準に、プレイ中の接触表示と根タップ精算表示の意味を分ける。ビーコン接触時は「命中確認：判定・経路種別」、正の精算差分は「得点確定：+N」、同じ経路の再発見は「発見済み（得点なし）」と表示する。凡例、初回案内、RULESにも「命中確認は接触時、得点は波の精算時」という流れを反映する。

得点式、波速110、物理、二段階台帳、30秒終了契約、保存、ランキング、Supabaseは変更しない。自動受入は表示文言、命中・精算のCSS区別、既知経路の0点表示、既存全試験、build、verify、配置分析、戦略分析、SVG、容量、差分検査、Draft ActionsのNode.js 18・20・22成功とする。実ブラウザ、iPhone・iPad、狭い画面での視認性、人の初見理解、Ready化、マージは未確認または未承認のまま残す。
# 現行Draft追補（10回タップ・水系効果音）

PR #67統合後のmain `72a3d5d97602c59d202b50c3b46605561f47ba6a`を基準に、次のDraftは反射後の波の輪を狙う技能タップを追加する。30秒、根波10回、波速110、寿命3秒、最大2回反射、得点基礎値、公式配置の座標・指紋・配置版、二段階台帳は維持する。経路上限10800点へ深度別の反射波タップ最大400点を別加算し、総hard ceilingを11200点へ更新する。client/rule/seasonは`namioshi-v3.4.0-official005` / `namioshi-v3-reflection-tap-004` / `prelaunch-v4`へ分離する。

タップ、壁・反射板反射、ビーコン命中、得点確定、RESULTに水系の短い合成cueを追加する。効果音は初期オフ、音なしでも読める画面文言、visibility/pagehide停止、ランキング無効を維持する。戦略分析は10入力の共通時刻列とsnapshotへ更新し、Draft作成後もマージしない。

## 27. 2026-08-15 PR #68統合後・反射波タップ視認性補助

PR #68のマージコミット`5a02f5aa337d848fca1a86f93bc94aaee9ba5c2f`を基準に、反射波タップの見つけやすさを補修する。未使用で寿命内の有限反射弧だけへ淡い黄色の補助表示を重ね、技能ボーナス取得後、根波確定後、寿命終了後は表示しない。

Worldの対象判定をWebGLとCanvas 2Dで共通利用し、補助表示が反射弧の有限範囲を隠さないこと、得点・判定範囲・物理・台帳を変更しないことを自動試験で固定する。実ブラウザ、iPhone・iPad、人の初見再現性、ランキング再開、Supabase適用、Ready化は未確認または未承認のまま維持する。

## 28. 2026-08-15 PR #69統合後・反射波タップのタイミング案内Draft

PR #69のmain `771ee93e0d2ff073b762a2095a048f9da6d1e9e0`を基準に、未使用の有限反射弧が現れた後のタップタイミングを、プレイ中の凡例内へ短く表示する。案内は黄色い反射弧へ重ねてタップすることと、深度別の+10〜40点を伝え、`World.isReflectionTapAvailable`が利用可能な波を返す間だけ表示する。案内中、PLAYING以外、技能ボーナス取得済み、根波確定済み、寿命終了後は非表示にする。

これは説明表示だけの変更であり、入力キュー、根波10回、反射タップ判定、得点、二段階台帳、物理、水系SE、ランキング無効状態を変更しない。Nodeテスト、build、verify、配置・戦略分析、SVG、容量、差分検査を自動受入へ含める。実ブラウザ、iPhone・iPad、初見再現性、ランキング再開、Supabase、Ready化、マージは未確認または未承認のまま残す。

## 29. 2026-08-16 PR #70統合後・反射波タップ案内の可視判定修正Draft

PR #70のマージコミット`790ae7fd09333e78a95fefaf90a23912c1168e49`を基準に、案内が表示されるのに黄色い反射弧が描画されない状態を修正する。ゲーム層へ描画側と同じ96個の中点サンプルによる可視判定を置き、案内・入力・Canvas 2D・WebGLが同じ条件を共有する。全体ボーナス上限10件も共有判定へ含める。

公式配置の`(140,520)`で、案内対象があるのに描画点が0になる固定stepを回帰試験へ追加する。得点、物理、判定範囲、二段階台帳、根波10回、水系SE、ランキング無効状態は変更しない。Nodeテスト、build、verify、配置・戦略分析、SVG、容量、差分検査を受入条件とする。実ブラウザ、iPhone・iPad、初見再現性、ランキング再開、Supabase、Ready化、マージは未確認または未承認のまま残す。

## 30. 2026-08-16 PR #71統合後・検収ゲートと正本同期Draft

Pull Request #71のマージコミット`4385d14c633c0d7d39c9b8b80b5fa2c277a6ff0c`を現行mainとして扱う。PR #71では、ゲーム側・入力・Canvas 2D・WebGLが同じ96中点サンプルによる可視反射弧判定を使い、反射タップ全体の上限10件も案内判定へ含めた。全151テスト、配布物整合性、配置・戦略分析、SVG、容量、差分検査、敵対的884ケース、GitHub Actions #126を確認した。

次のDraftでは、PR #71後の現行main、10回タップ、30秒、波速110、hard ceiling11200点、未確認の実ブラウザ・実機検収範囲をCURRENT_TASK、README、レビュー表、Phase 8B記録へ同期する。ゲームコード、得点、物理、入力、描画、音、保存、ランキング、Supabaseは変更しない。実ブラウザ・iPhone・iPad・初見再現性・長時間動作・ランキング再開・Ready化は未確認のまま残す。


## 31. 2026-08-16 PR #73統合後・次期検収ゲート

PR #73のマージコミット`148ff54ea516f24ffe02501fdb4797ff915adad5`を次の検収基準とする。波は約3秒で自然に消え、タップで既存波を削除せず、根波10回後に入力と物理波が空になれば早期にRESULTへ進む。自動テスト153件、build、dist検証、配置・戦略分析、レイアウト確認、容量確認、GitHub Actions G2 Build Verification #131は成功した。

次の工程は、実ブラウザで10回後の終了表示とRESULT遷移を確認し、iPhone・iPadの波の視認性、初見操作の反射経路理解、水系SEの聞き分けを検収することである。ランキング、Supabase、Ready化、公開判定は確認完了まで停止する。未確認を自動試験済みとは扱わない。


## 0. 2026-08-17 現行mainの状態（PR #74統合後）

この章を現在判断の基準とする。後半のPR別追補は履歴であり、同じ項目について古い記述が残る場合は、この章と最新追補を優先する。

- 基準main: `a0d3f3c2f8a6d90579b71b62e1df813a56886057`
- 実装・自動検証済み: 30秒、根波10回、波寿命約3秒、表示・反射波タップ対象最大10秒、既存波の保持、10回後の表示中波待機、反射経路と二段階台帳、反射波タップ技能ボーナス、画面案内、水系SE、Canvas 2D fallback、155テスト、build・dist・配置・戦略・容量検査。
- 受入未確認: 実ブラウザでの終了表示、iPhone・iPadでの波の視認性と終了タイミング、初見プレイの反射経路理解、水系SEの聞き分け、長時間・反復・休止復帰・発熱。
- 停止中: ランキング、Supabase実通信、公式記録の再開、公開判定、Ready化。
- この追補でコード、得点、物理、入力、描画、音、ランキング、Supabaseは変更しない。

## 2026-08-17 PR #77 Draft・反射波表示10秒とプレイ説明トグル

PR #77では、反射子波の物理・得点寿命を3秒のまま保ち、表示と反射波タップの対象だけを最大10秒へ分離した。根波10回、得点式、経路台帳、hard ceiling11200点は維持する。カウントダウン中は説明を表示し、PLAYING中は上部ボタンから任意に開閉する。表示中の反射輪が残る間は早期RESULTへ進めない。

G2 Build Verification #164はNode.js 18・20・22の全ジョブ成功（155テスト、build、verify、配置・戦略分析、SVG、容量、dist差分検査）。PR #77は`a7bc11fa9c4fd109541f5dfc844a6793afc02da8`としてmainへ統合済みである。実ブラウザ、iPhone・iPad、初見再現性、10秒表示の視認性と終了タイミングは未確認、ランキング再開・Ready化・公開判定は停止中とする。


## 32. 2026-08-17 PR #77統合後・次期受入ゲート

- 現行main: `a7bc11fa9c4fd109541f5dfc844a6793afc02da8`（PR #77マージ済み）。
- 完了: 根波10回、反射子波の物理・得点寿命約3秒と表示・技能タップ対象最大10秒の分離、COUNTDOWN説明、PLAYING説明トグル、10回後の表示中波待機、競技ルール版分離。
- 自動検証: G2 Build Verification #164をNode.js 18・20・22で成功（155テスト、build、verify、配置・戦略分析、SVG、容量、dist差分検査）。
- 未確認: 実ブラウザでの10秒表示・反射波タップ・RESULT遷移、iPhone・iPadの視認性、初見3〜5プレイの理解・再現、水系SEの聞き分け、長時間・反復・休止復帰・発熱。
- 停止: ランキング、Supabase SQL/RPC・本番疎通、公式記録、Ready化、公開判定。
- 次のDraftは文書と検収状態の同期だけを行い、ゲームコード、得点、物理、入力、描画、音、保存、ランキング、Supabaseを変更しない。実機結果が得られた場合は対象コミットと手順をPhase 8Bへ記録する。


## 33. 2026-08-17 PR #78統合後・iPhone 17 Pro受入

- 現行main: `a0d3f3c2f8a6d90579b71b62e1df813a56886057`（PR #78マージ済み）。
- 自動検証: G2 Build Verification #166はNode.js 18・20・22で全ジョブ成功。155テスト、build、verify、配置・戦略分析、SVG、容量、dist差分検査を通過した。
- 次の確認対象はiPhone 17 ProのSafariに限定し、目的・ビーコン・反射板・反射輪タップ・10回後のRESULT・水系SE・説明トグル・狭い画面の視認性を確認する。
- 実機確認を自動試験済みへ置き換えない。実施していない項目は未確認、再現可能な不具合が得られた時だけコード修正へ進む。
- ランキング、Supabase、公式記録、Ready化、公開判定は受入結果とユーザー判断まで停止する。
