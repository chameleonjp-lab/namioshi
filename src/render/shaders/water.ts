export const vertexShader=`varying vec2 vUv;void main(){vUv=uv;gl_Position=projectionMatrix*modelViewMatrix*vec4(position,1.0);}`;
export const fragmentShader=`precision mediump float;varying vec2 vUv;uniform float uTime;uniform vec2 uResolution;uniform int uWaveCount;uniform vec4 uWaves[12];void main(){vec2 p=vUv*uResolution;float w=0.0;for(int i=0;i<12;i++){if(i>=uWaveCount)break;vec4 a=uWaves[i];float d=distance(p,a.xy);float line=exp(-pow(abs(d-a.z)/(a.w+1.0),2.0));w+=line*.22;}float rip=sin((p.x*.045)+(uTime*1.4))*0.025+sin((p.y*.055)-(uTime*1.1))*0.018;vec3 col=mix(vec3(.006,.025,.055),vec3(.02,.16,.24),vUv.y+rip+w);col+=vec3(.06,.55,.75)*w;gl_FragColor=vec4(col,1.0);}`;
